# JRN — S3 drive (real Borland headers)

Running log for the autonomous drive to complete **stone S3** of
`wrk_docs/2026.05.26 - PLAN - Ultimate goal (BCC 4.52 conformance).md`.
Successor to `2026.05.27 - JRN - S2 drive (32-bit x86 backend).md`.

## Predecessor handoff — *** STONE S2 LANDED *** (2026-05-28)

S2 (32-bit x86 backend) closed at commit `ee147a1`. The i386/Win32 backend
compiles real C + a broad C++ subset and runs natively on Win11 x64
(WOW64) matching the bcc32 4.52 reference:

- **O12: i386 e2e MATCH 83/88, 0 MISMATCH, 0 COMPILE-GAP.** The 5
  non-matches are bcc32's OWN failures on this rig (REF-GAP) — mdbcc
  matches 100% of the fixtures bcc32 can itself build (exit + stdout).
- 24 ticks, every gate supervisor-verified; an independent reviewer gate
  found no Critical issues and 2 High items (both fixed). The 88 x64
  SipHash baselines + pe_imports goldens stayed byte-identical throughout;
  an x86 SipHash byte-identity stripe locks the i386 output. `cargo test`
  1194 green; `cargo clippy -D warnings` clean.
- Capabilities: scalar + all int ops, control flow, recursion, cdecl
  calls (fn-ptrs, ref params, member/virtual dispatch), globals + absolute
  addressing, **ILP32 4-byte pointers + Borland byte-aligned structs**,
  printf (int/float/string/padded), libc string/mem builtins, RAII dtors,
  basic `try/throw/catch(int)` fs:[0] SEH.

**S2 follow-ons deferred (S5/OWL-prep, NOT S2 blockers):** full EH (class
throws / nested / rethrow / dtor-unwind); S2b.6 `_name` cdecl decoration
(O13/O14 cross-toolchain — needed in S5 to link Borland `.lib`s). These
can fold into S4/S5 opportunistically.

## S3 goal (plan §S3)

Stop using stub headers; mdbcc's preprocessor reads `\BC45\INCLUDE\`
directly. **Exit**: a Petzold-style Win32 sample (`HELLOWIN.C`, ~150 LoC,
real `<windows.h>`) builds + runs as 32-bit; **O15** — every `.h` in
`\BC45\INCLUDE\` parses through `mdbcc -E` without error (preprocessor
acceptance; semantic type-check is S4/S5).

## S3 readiness probe (2026-05-28, supervisor)

- `wrk_oracle/bc452/BC45/INCLUDE/` holds **246 headers** (ALLOC.H … CTYPE.H
  … WINDOWS.H …) + a `CLASSLIB/` subdir. The real corpus is on disk.
- **mdbcc has no `-I` (include search path) and no `-E` (preprocess-only)
  flag** — the driver only does `--dump-tokens` / `-c` / `-o` / `-m32`.
  `bcc -c -m32 -I <dir> x.c` errors "unknown option '-I'". So mdbcc can't
  yet be pointed at the real headers at all.
- The preprocessor (`src/pp.rs`) today resolves `#include "..."` relative
  to the TU dir and **stubs `<...>` system headers** (the libc subset is
  intrinsic). S3 must replace the stub path with real `<...>` search over
  the `-I` paths.

## Recommended S3 first steps (for the next iteration — re-baseline here)

1. **Author `wrk_docs/2026.05.?? - HLD - S3 real headers.md`** (design pass,
   per the operating model's per-stone HLD). Cover: the `-I`/`-E` driver +
   resolver changes; `#include <...>` search order (system paths then
   user); full **hide-set macro expansion** (X3J11 algorithm, replacing
   the re-include-protect hack); `#pragma once`/`hdrstop`/`pack(push/pop,
   n)`/`argsused`/`warn`; the **BCC predefined macro set** (`__BORLANDC__`,
   `__BCPLUSPLUS__`, `__TURBOC__`, `_WIN32`/`__WIN32__`, `__FLAT__`,
   `__CONSOLE__`, `__MSDOS__`=off for 32-bit — match `\BC45\DOC\`); and the
   C-language gaps real headers surface (`_pascal`/`_cdecl`/`_stdcall` +
   leading-underscore variants as keywords — the lexer already tokenises
   these (S2b.3); `_far`/`_near`/`_huge` as 32-bit no-ops; `__declspec(
   dllimport/dllexport)`; bit-fields with non-int base; `__interrupt`;
   anonymous structs/unions).
2. **`-I <path>` + `-E` driver flags** (`src/main.rs`) + wire `-I` into a
   real system-header resolver in `src/pp.rs` (`<...>` searches the `-I`
   paths in order). `-E` emits the preprocessed token stream. Bounded,
   testable first tick.
3. **Surface-driven O15 sweep**: `mdbcc -E -I \BC45\INCLUDE\` over each of
   the 246 headers; fix preprocessor gaps as they surface (pragmas,
   predefined macros, hide-set bugs). Add an O15 oracle test (every header
   preprocesses without error) + the S3 SHA-locked token-stream stripe
   per the May-27 D-S3-Stripe decision (SHA-256-lock each header's token
   stream once O15 is green — cheap insurance against preprocessor
   regressions).

Then S3 exit: HELLOWIN.C (real `<windows.h>`) builds + runs 32-bit.

---

## Tick log

Every tick supervisor-verified (88 x64 SipHash byte-identical, full
`cargo test` green, `clippy -D warnings` clean). O15 = ACCEPT count over
the 246 real `\BC45\INCLUDE\` headers via `tests/oracle_o15_header_
acceptance.rs` (ratcheting `FLOOR_O15`). Subagents did the heavy work;
supervisor re-ran every gate.

- **S3.1 `d90b9b5`** — driver `-I <dir>`/`-Idir` + `-E` (preprocess-only)
  + `SearchPathResolver` (case-insensitive `<...>` search of `-I` dirs,
  stub fallback) + the O15 harness. With no `-I`, the legacy
  DefaultResolver is byte-identical, so all existing tests are unaffected.
  **O15 baseline 116/246.**
- **S3.2 `166dd89`** — fixed function-like-macro detection: `adjacent()`
  now requires the `(` to be col-adjacent to the macro name
  (`b.col == a.col + spelling(a).len()`), so spaced object-macros like
  `#define EOF (-1)` are no longer mis-parsed as function-like. Shared
  no-`-I` path; 88 SipHash byte-identical. **O15 116 → 208** (the single
  biggest lever).
- **S3.3 `295ecac`** — coupled lexer + macros. Lexer: a `'` with no
  closing quote before EOL is a pp-token (`TokenKind::Other(u8)`), not a
  hard error (real headers' `#error Can't include...`). pp: BCC predefined
  macros (bcc32-probed: `__BORLANDC__`=`__TURBOC__`=**0x0460**;
  `__WIN32__`/`__FLAT__`/`__CONSOLE__`/`__TLS__`/`_Windows`=1; NOT
  `_WIN32`/`__MSDOS__`). `__cplusplus`/`__BCPLUSPLUS__` deferred (flip
  `extern "C"` on). **O15 208 → 215.**

**O15 @ `295ecac`: 215/246.** Remaining 31 rejects: ~21 C++-mode `#error`
(need `__cplusplus` + `extern "C" { }` block parsing — a coupled tick);
~10 lines with a trailing `0x1A` (Ctrl-Z DOS-EOF) byte the lexer rejects;
a few needing other macros (objbase ordering, W32SUT, BGI-on-Windows).

- **S3.4 `e77674b`** — lexer treats a token-start `0x1A` (Ctrl-Z) as
  logical DOS-EOF (Borland behaviour), recovering the ~10 trailing-Ctrl-Z
  headers. **O15 215 → 225.** 88 SipHash byte-identical.

**O15 @ `e77674b`: 225/246.** The remaining **21 are all C++-only headers
that deliberately `#error "Must use C++"` behind `!defined(__cplusplus)`**
— i.e. CORRECT rejections of C-mode inclusion (the header doing its job).
So effectively every C-includable header now preprocesses. To ACCEPT the
21, preprocess them in C++ mode (`__cplusplus` defined) — which is the
next item and is coupled to `extern "C" { }` block parsing (those headers
wrap declarations in it).

- **S3.5 `66bb834`** — mode-aware `__cplusplus`=1 / `__BCPLUSPLUS__`=0x0340
  (threaded `cplusplus: bool` through `pp::preprocess`; compile path passes
  `false` = C mode → x64 SipHash byte-identical; O15 harness passes `true`).
  **O15 225 → 241/246.** The 5 remaining `#error` standalone the SAME way
  bcc32 does (GRAPHICS/INITGUID/MSACM/MAPIWIN/W32SUT — header-ordering/
  platform context), so **241/246 is the effective O15 ceiling — the S3
  O15 criterion is MET.**

### *** S3 O15 criterion MET (241/246) — remaining: HELLOWIN.C ***

The other S3-exit criterion (a Petzold sample on real `<windows.h>` builds
+ runs 32-bit) is the big semantic step. **Probe @ `66bb834`:**
`#include <windows.h>` preprocesses to **99,436 tokens**, but `mdbcc -c
-m32 -I <INCLUDE>` then fails in the PARSER (first gap: `error: expected a
declared name`). So the endgame is **parsing real `<windows.h>`** — a long
surface-driven loop (99k tokens of typedefs/structs/prototypes/macros will
surface many C-declaration-form gaps: `__declspec`, calling-conv keywords
mid-declarator, fn-pointer typedefs, anonymous/nested structs, bit-fields,
`extern "C" { }` blocks, etc.), THEN the Win32 API call/import + WinMain/
message-loop link. This is the substantive remaining half of S3 and a
fresh multi-tick effort. Error positions currently map to the PREPROCESSED
stream — a `-E` dump + locating the offending construct is the first step
each tick. **NB: `extern "C" { }` block PARSING is needed here** (deferred
from S3.5; real C headers wrap decls in it under `#ifdef __cplusplus`).

- **S3.6 `851844c`** — first REAL-header COMPILE: a program over real
  `<string.h>`+`<stddef.h>`+`<stdlib.h>` compiles (`mdbcc -c -m32 -I
  <INCLUDE>`), links, runs, matches bcc32 (exit 6). 4 additive parser/
  lexer/codegen fixes: `T * __cdecl name(...)` (conv between stars and
  name), grouped fn-ptr typedef with inner conv, `wchar_t` as a C
  identifier (removed dead keyword), and `is_builtin` so a header's bare
  prototype doesn't defeat the libc intrinsic. New
  `tests/oracle_real_headers.rs` with a real-header compile test + a
  **parse-acceptance ratchet: PARSE-OK 78/246 (`FLOOR_PARSE_C`=78)** — the
  HELLOWIN endgame metric. 88 x64 SipHash + cpp_*/end_to_end/mangling
  byte-identical.

### S3 endgame status — the parse-acceptance climb to HELLOWIN.C

Two S3-exit criteria: **O15 preprocessor-acceptance = MET (241/246)**;
**HELLOWIN.C on real `<windows.h>` = the remaining half**. Progress is now
measured by the C-mode **parse-acceptance ratchet (78/246 @ `851844c`)**.
The climb to 246 (and then `<windows.h>`, which needs the most) is a
surface-driven parser loop — the remaining 168 headers' gaps (per the
ratchet's printed grouping) are: **`extern "C" { }` / `extern "C++"`
linkage blocks** (very common), **`__declspec(dllimport/dllexport)`**,
**anonymous & nested struct/union members**, **bit-fields**, and assorted
declarator forms. Each is an additive parser tick (keep the 88 x64
fixtures byte-identical). Once parse-acceptance is high enough to parse
`<windows.h>` + WINDEF/WINNT/WINBASE/WINUSER/WINGDI, wire the Win32 API
imports + WinMain/message-loop and build/run HELLOWIN.C vs bcc32.

- **S3.7 `ca6f7e8`** — `extern "C"`/`extern "C++"` linkage-spec parsing
  (block + single; `Parser.c_linkage` threaded, save/restored for nesting;
  normal `extern` path untouched). Additive → 88 SipHash + mangling +
  end_to_end byte-identical. C-mode ratchet unmovable (extern "C" is
  `#ifdef __cplusplus`-gated), so added a **C++-mode parse ratchet:
  PARSE-OK(C++) 47 → 77/246 (`FLOOR_PARSE_CXX`=77)**.

### S3 → S4 inflection (repo pristine at `ca6f7e8`)

S3's preprocessor + C-toolchain goals are **met**: O15 241/246 (ceiling);
real C headers (`<string.h>`/`<stddef.h>`/`<stdlib.h>`) compile + run +
match bcc32; `extern "C"` parses. Two distinct remaining threads:

1. **windows.h / HELLOWIN.C (pure S3, C-mode)** — `<windows.h>` is C; it
   needs more C declaration-form parsing (the C-mode probe failed at
   "expected a declared name"; gaps incl. `WINAPI`/`CALLBACK`→`__stdcall`
   in declarators, `__declspec`, struct/typedef forms, fn-ptr params).
   Then wire the Win32 API imports + WinMain/message-loop + build/run vs
   bcc32. C-mode parse ratchet `FLOOR_PARSE_C`=78 tracks this.
2. **C++ headers (merges into S4)** — the dominant C++-mode gap is
   "expected a type" (140 headers): class/template/`::`-qualified
   constructs — i.e. **S4 territory** (templates/MI/RTTI, the plan's long
   pole, pre-scoped by D-Templates-S4). S4 warrants its own HLD + design
   pass.

So the natural next moves are: (a) push the C-mode parse-climb to get
`<windows.h>` parsing → HELLOWIN.C (closes S3's second exit criterion);
(b) open S4 with an HLD for templates/classes (the C++-header parsing +
the OWL endgame depend on it). Either is a fresh multi-tick effort.

- **S3.8 `2375b3a`** — C-mode parse-climb **78 → 111/246**, and **real
  `<windows.h>` (99k preprocessed tokens) now PARSES** (reaches the
  semantic main/WinMain check). Additive forms: bit-fields (syntax;
  packing deferred), `__import`/`__export` runs before `*` in grouped
  declarators (`WINAPI`==`__stdcall __import`; new `Import` keyword),
  east-const, function-type typedefs (`typedef RET NAME(params)` etc.),
  anonymous struct/union members (syntax; promotion deferred). FLOOR_PARSE
  C 78→111, C++ 77→79. **Key finding:** the ~135 remaining C-mode rejects
  are mostly NOT parser gaps — Win32-SDK headers that don't re-`#include
  <windows.h>` standalone (so their `UINT`/`HWND` are correctly undefined;
  ~81 more parse with windows.h pre-included). 88 SipHash + mangling +
  end_to_end byte-identical.

### *** S3 status: preprocessor + real-header PARSING done — HELLOWIN.C codegen finale remains ***

Both hard S3 parsing/preprocessing fronts are essentially won: O15 241/246
(ceiling), real C headers compile, `<windows.h>` parses. The last S3-exit
criterion — **HELLOWIN.C (a Petzold Win32 GUI sample on real
`<windows.h>`) builds + runs 32-bit matching bcc32** — is now a
CODEGEN+LINK+SEMANTICS effort, not a parse effort:
- **GUI subsystem + WinMain entry** (vs console `main`) — the linker's
  entry-stub/CRT for `Subsystem::Gui` + `WinMain` (Phase C already has a
  GUI stub; wire it for the real WinMain signature).
- **Win32 API imports** — HELLOWIN.C calls ~15-20 USER32/GDI32 functions
  (`RegisterClassEx`, `CreateWindow`, `ShowWindow`, `GetMessage`,
  `TranslateMessage`, `DispatchMessage`, `DefWindowProc`, `BeginPaint`/
  `EndPaint`, `TextOut`, `PostQuitMessage`, …). Extend `WIN32_IMPORTS`
  (USER32.DLL/GDI32.DLL groups) + resolve them via the existing IAT path.
  The window-proc is a `CALLBACK` (`__stdcall`) function — pointer passed
  to the API.
- **Deferred semantics now needed**: real bit-field LAYOUT + anonymous-
  struct member ACCESS (windows.h structs like MSG/PAINTSTRUCT/RECT are
  fine, but some use bit-fields/anon unions).
- **Verification**: a GUI program isn't a console exit/stdout diff — use
  the O6 liveness oracle / O17 (mdscreensnap diff vs the bcc32-built
  HELLOWIN + scripted WM_ injection per plan §S6). Heavier harness.

This is a fresh multi-tick effort (GUI/Win32-API integration). In
parallel, the C++-header parse-climb (the "expected a type" gaps) IS the
**S4** work (templates/MI/RTTI/classes, the plan's long pole) and wants
its own HLD.

- **S3.9 `66a6ca6`** — i386 GUI `WinMain` entry: PE32 GUI entry stub
  (is_pe32-gated; pushes 4 args, `call WinMain`, `add esp,16`, ExitProcess).
  Minimal `__stdcall WinMain` AND real-`<windows.h>` `int WINAPI
  WinMain(HINSTANCE,...)` both compile → GUI PE32 → exit 42 vs bcc32.
  **Real `<windows.h>` COMPILES (not just parses)** for the WinMain
  signature. x64 GUI suites byte-identical.
- **S3.10 `a62bf8c`** — true `__stdcall` callee-cleanup (`ret N`): `Sigs.
  convs` map (empty for non-stdcall programs → 88 baselines unaffected);
  callee `leave; ret N`; caller skips `add esp`. The window-proc-callback
  prerequisite. 1000-call stack-balance test vs bcc32 (=232).
- **S3.11 `bc6f5ca`** — i386 Win32 API call marshalling. `emit_win32_call`
  (the recognized-DLL-import path) marshalled args via the Win64 register
  convention on BOTH targets — wrong on i386 (Win32 APIs are `__stdcall`).
  Only `emit_write_buf` hand-coded the stdcall sequence; the GENERAL path
  was the gap (confirmed). Fix (gated; Win64 verbatim): Win32 marshals via
  `marshal_args_cdecl` (push right-to-left) + `call dword ptr [IAT]`
  (`FF 15`→Addr32) + NO caller cleanup (callee-clean DLL). `WIN32_IMPORTS`
  += `lstrlenA`,`lstrcmpA` AND `WIN32_IMPORTS_X64_EXCLUDE` += same (x64
  non-pruning grouping skips them → 88 SipHash + pe_imports byte-identical;
  PE32 prunes per-symbol so i386 imports only-when-used). 2 tests vs bcc32:
  `lstrlenA("hello")`=5; sign-encoded `lstrcmpA` triple=7 (proves R→L push
  order — reversed would give 4). **i386 can now call Win32 APIs.**
- **S3.12 `d17e9b0`** — i386 (PE32) window-surface structural oracle
  (`tests/i386_window_surface.rs`), the 32-bit twin of
  `win32_window_surface.rs`. The canonical hello-window program
  (RegisterClassExA→CreateWindowExA→GetMessageA/Translate/Dispatch loop→
  `__stdcall` WndProc→DefWindowProcA) compiles to a valid i386 **PE32**
  GUI image: Machine 0x14C, Magic 0x10B, Subsystem 2, KERNEL32+USER32
  descriptors with the full 12-symbol D1 USER32 set via **4-byte** ILT
  thunks. Proves the pieces COMPOSE on i386: S3.11 stdcall path
  generalises to the whole USER32 surface; intrinsic `<windows.h>`
  resolves WINAPI/CALLBACK→`__stdcall` (WinMain→`_WinMain@16`,
  auto_subsystem→GUI); S3.10 callback + struct-by-ptr args lower. + PE32
  console dormancy mirror (plain `main` → KERNEL32 only). Self-contained
  PE32 parse helpers (data dir @opt+96; dynamic SizeOfOptionalHeader);
  test-only, no `src/` change → 88 SipHash definitionally unaffected.
  **Structural side of HELLOWIN now done on BOTH targets.**
- **S3.13 `d77a60d`** — entry-point detection counts DEFINITIONS only.
  A program that merely `#include <windows.h>` (real Borland header) +
  `int main()` failed with "both main and WinMain; pick one": real
  `WINDOWS.H:285` *declares* `int PASCAL WinMain(...)`; the parser folds
  body-less prototypes into `sigs.funcs` (for call-mangling), so keying
  entry selection off `sigs.funcs.contains_key` saw the WinMain PROTOTYPE.
  Fix (minimal, byte-identical): count only `Item::Func` definitions in
  `tu.items` (parser stores definitions there, prototypes in a separate
  `extern_protos` channel) → a prototype can never select/collide on an
  entry. Identical Entry for all 88 baselines → bytes unchanged.
  **MILESTONE: `bcc -c -I \BC45\INCLUDE` on `#include <windows.h>`+main
  produces a valid COFF object on BOTH targets (i386 9083 B / x64 9088 B)
  — mdbcc parses + codegens the WHOLE real windows.h closure
  (windef/winnt/winbase/wingdi/winuser/…).** The direct S3-exit
  prerequisite for compiling HELLOWIN.C against real headers.
- **S3.14 `6a41117`** — **`switch` / `break` / `continue`** statements.
  mdbcc had NONE of them (lexer tokenised the keywords; parser/AST/codegen
  never handled them) → `switch(message){…}` (every WndProc's shape) gave
  "expected an expression". AST: `Stmt::{Switch,Case,Default,Break,
  Continue}`; switch body is a flat Block interleaving Case/Default markers
  with statements (C fall-through). Codegen (target-agnostic — rel32 jumps
  + `cmp eax,ecx` work on both): linear compare-and-jump dispatch
  (scrutinee→temp once; per case `eax=Vi; cmp eax,[E]; je`; constants so no
  const-evaluator; fallback→default else end); break/continue via
  per-construct patch-handle frames (`break_stack`/`continue_stack`) — a
  loop/switch with none emits ZERO extra bytes; `continue` in a switch
  targets the enclosing LOOP (loops push both frames, switch pushes break
  only). **Byte-identical** (new constructs; empty frames emit nothing →
  88 SipHash + x86 stripe unchanged). 16 tests: 11 x64 (end_to_end) + 5
  i386 (diff-tested vs bcc32) incl. fall-through, default-in-middle,
  char-switch, continue-in-switch-targets-loop. Full 1263 green; clippy
  clean. **MILESTONE: full Petzold HELLOWIN.C (WNDCLASS+CreateWindow+
  message loop+switch WndProc) compiles to a COFF object vs the REAL
  `<windows.h>` on BOTH targets (i386 11941 B / x64 12136 B).**
- **S3.15 `a9b8af5`** — **HELLOWIN.C COMPILES + LINKS vs the real
  `<windows.h>` → valid i386 PE32 GUI image** (S3-exit compile/link
  milestone). Three independent pp/codegen bugs, each surfaced by driving
  the real sample:
  1. **pp cross-boundary macro rescan** (C99 §6.10.3.4). `expand_inner`
     expanded an object-macro body in isolation, so a replacement ending in
     a func-like macro NAME never saw the following source `(...)`. Real:
     `#define MAKEINTRESOURCE MAKEINTRESOURCEA` → `IDC_ARROW =
     MAKEINTRESOURCE(32512)`. New `rescan_boundary_call` splices the pending
     call (loops for A→B→C(...)); strict no-op otherwise ⇒ byte-identical.
  2. **pp multi-line macro calls**. `run` expanded each logical line alone →
     a call whose `(...)` crossed a newline gave "unterminated macro
     argument list" (HELLOWIN's `CreateWindow(...)` spans 5 lines). `run`
     now expands a maximal run of CONSECUTIVE non-directive lines as one
     unit (a directive can't sit inside an arg list; `active` constant) ⇒
     same tokens as per-line for non-cross-line text ⇒ byte-identical.
  3. **codegen `is_win32_import` counted a PROTOTYPE as a definition**. Real
     `<windows.h>` *declares* every API; the parser folds body-less protos
     into `sigs.funcs`, so `!sigs.funcs.contains_key` mis-saw every declared
     API as user-defined → unresolved `_LoadIconA` instead of IAT routing.
     Now only a true DEFINITION (in `sigs.funcs` but NOT `extern_symbols`)
     overrides; a prototype stays importable. (Same shape as S3.13.)
  + `WIN32_IMPORTS` += LoadIconA/RegisterClassA/GetClientRect/DrawTextA
  (USER32) + GetStockObject (GDI32), all i386-only via `X64_EXCLUDE` (PE32
  prunes per-symbol; x64 grouping skips them → every x64 GUI `.idata`
  byte-identical). Tests: `tests/i386_hellowin.rs` (real headers, self-skip;
  links in-process → PE32, asserts machine 0x14C/Magic 0x10B/Subsystem 2 +
  all 16 HELLOWIN imports across KERNEL32+USER32+GDI32) + 3 pp regression
  tests. **Full 1267 green; clippy clean; 88 SipHash + x86 stripe
  byte-identical; O15 + parse floors held.** Known follow-up (NOT
  S3-blocking): mdlink CLI `.obj` round-trip doesn't serialise Win32 imports
  (affects lstrlenA too); the in-process linker (every i386 test + the
  driver's intended path) handles them — filed for x64-GUI/driver polish.

- **S3.16 `39a830e`** — **HELLOWIN.C GUI-liveness tripwire** (the liveness
  half of Arthur's Structural+tripwire). `tests/i386_hellowin.rs` now builds
  the real-`<windows.h>` i386 PE32, writes a temp `.exe`, launches it under
  WOW64, polls `FindWindowA` for a top-level **"HelloWin"** window belonging
  to OUR child pid (`GetWindowThreadProcessId`), posts `WM_CLOSE`
  (→DefWindowProc→DestroyWindow→WM_DESTROY→PostQuitMessage(0)→loop exits→
  WinMain returns wParam), and asserts a clean **exit 0** (no crash STATUS).
  **Verified the liveness path actually executed (no skip): window found,
  closed, exit 0.** End-to-end RUNTIME proof — WinMain stub + stack WNDCLASS
  by ptr + `__stdcall` WndProc + switch dispatch + USER32/GDI32 IAT calls
  all work together as a functioning native 32-bit GUI app. Tripwire
  (self-skips headless/WOW64-refusal; a crash-before-window is still a hard
  red; RAII kill-on-drop watchdog). Test-only ⇒ 88 SipHash + x86 stripe
  unchanged; clippy clean; full 1268. (cli_dash_c's external `lld-link`
  test is load-flaky under the full parallel suite — passes in isolation +
  on re-run; unrelated to mdbcc codegen.)

## *** STONE S3 LANDED *** (2026-05-29)

**S3 EXIT criterion met: the Petzold HELLOWIN.C — built by mdbcc from the
REAL Borland `<windows.h>` (`-m32 -I \BC45\INCLUDE`) — compiles, links to a
valid i386 PE32 GUI image, and RUNS natively on Win11 x64 (WOW64): it
registers its class, creates + shows its window, pumps messages, and exits
cleanly on WM_CLOSE.** Real `<windows.h>` (windef/winnt/winbase/wingdi/
winuser, thousands of decls) now both preprocesses (O15 241/246) AND
compiles+codegens on both targets. The drive surfaced + fixed, RED→GREEN,
each gate verified: i386 Win32 API stdcall marshalling (S3.11), i386 window
surface (S3.12), entry-detection definitions-only (S3.13), **switch/break/
continue** (S3.14 — mdbcc had none), HELLOWIN compile+link via 3 pp/codegen
fixes — cross-boundary macro rescan + multi-line macro calls + import
prototype-guard (S3.15), and the GUI-liveness tripwire (S3.16). Every tick
kept the 88 x64 SipHash + x86 stripe byte-identical; full suite 1066→1268.

Next stone: **S4 (templates / multiple inheritance / RTTI)** — the long
pole; wants its own HLD + journal. Then S5 (build OWL) · S6 (OWL samples)
· S7 (64-bit native).

### S3 endgame (continuation — repo pristine at `a62bf8c`)
1. **`__cplusplus`/`__BCPLUSPLUS__` + `extern "C" { }` parsing** — define
   the C++-mode predefined macros (mode-aware: only for `.cpp`/C++ TUs)
   and parse `extern "C" { ... }` linkage blocks (mdbcc currently lacks
   this; real C headers wrap their decls in it). Unblocks the 21 + is
   needed to COMPILE (not just preprocess) any real header in C++ mode.
2. **S3 EXIT — HELLOWIN.C**: a Petzold ~150-LoC Win32 sample using the
   REAL `<windows.h>` builds (`mdbcc -m32 -I \BC45\INCLUDE\`) + links +
   runs 32-bit, matching bcc32. **This is the big semantic milestone** —
   real `<windows.h>` pulls in thousands of typedefs/structs/prototypes/
   macros (WINDEF/WINNT/WINBASE/WINUSER/WINGDI…), so it exercises far more
   of the parser + type system than the stub path ever did. Expect many
   surface-driven C-language/semantic gaps (the plan §S3 predicted:
   `__declspec`, calling-conv keywords in declarators, bit-fields,
   anonymous structs/unions, `__interrupt`, etc.). Drive it surface-first
   against the bcc32 differential.
3. **S3-stripe** (D-S3-Stripe): SHA-lock each header's `mdbcc -E` token
   stream once O15/compilation is stable — cheap regression insurance.

Then S4 (templates/MI/RTTI) · S5 (build OWL) · S6 (samples) · S7 (64-bit).
