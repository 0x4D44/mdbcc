# JRN — Phase A calling path: COLLISION + STAND-DOWN

Date: 2026-05-17. Autonomous continuation (Arthur overnight:
"push on, sensible decisions, journal as you go").

## What happened (honest record)

After completing & pushing the commit-organization task, I picked
the next plan-of-record item — **Phase A (>4 args + function
pointers)** — designed it independently, confirmed the RED oracle
(`tests/calling.rs`, 9/9 failing as expected), wrote a design, and
began implementing A1 in `src/codegen.rs`.

**My first `Edit` to codegen.rs failed: "File has been modified
since read."** Re-reading revealed a **concurrent engineer is
actively implementing the very same Phase A** in `src/codegen.rs`:
- New `Gen` fields `frame_fixed`, `io_out`, `max_stack_args`.
- 4-param hard error already removed.
- Prologue already receives args ≥4 from `[rbp+16+8*i]`.
- Dynamic `sub rsp` imm32 re-patch for outgoing stack-arg space.
- They also authored `wrk_docs/2026.05.17 - HLD - Phase A
  (gt4 args + function pointers).md` and the RED suite.

Their design is independent-but-near-identical to mine and clean.
(My failed Edit was NOT applied — codegen.rs carries only their
work, not mine. Verified via git: codegen.rs 56+/16−, none mine.)

## Decision: STAND DOWN from Phase A

CLAUDE.md multi-agent rules are explicit: don't conflict with
others' in-flight work; only modify code you authored or were
asked to; don't barge into files another engineer is mid-edit on.
Two agents implementing the same feature in the same file =
guaranteed mess. The correct senior-engineer move is to **not make
it worse**.

"Make sensible decisions to avoid getting blocked" ≠ do redundant
or conflicting work. Standing down here IS the sensible decision,
made decisively (not blocked): Phase A is owned and progressing.

Considered and rejected as unsafe/low-value right now:
- Parallel-implementing Phase A → direct collision. NO.
- Starting Phase B (virtual fns) → also heavy codegen.rs/parser.rs/
  ast.rs; collides with their uncommitted codegen.rs; can't cleanly
  work that file. Premature. NO.
- Adding `tests/corpus/portable/{manyargs,funcptr}.c` → the
  calling.rs header names these as part of the Phase A deliverable;
  they'll almost certainly add them. Risk of churn. HOLD.
- Editing `notes/scratchpad.md` stale-PE-note → shared file the
  concurrent agent likely also appends to. Don't touch. HOLD.

Almost all substantive compiler work touches the hot files
(codegen/parser/ast) now dirtied by their uncommitted Phase A.
The honest call: do NOT spin up speculative/conflicting work just
to look busy (CLAUDE.md quality philosophy: code you didn't write
is the cheapest; premature work resists the correct design — the
M8 ring-road on-ramp-to-nowhere).

## Net

- Original task (commit-organization + push): COMPLETE & pushed
  (commits 0e061d6, dc74e83, d85b1d6; origin/main + branch synced).
- Phase A: correctly delegated-by-circumstance to the active
  concurrent engineer. I produced an independent design (kept below
  for cross-check value only — NOT to be implemented by me).
- My footprint on shared code: ZERO (no src/* changes by me).
- Journals committed separately (disjoint files; safe; atomic).

## My independent Phase A design (cross-check reference only)

[Retained for the owning engineer to diff against their HLD if
useful — I am NOT implementing this.]
- A1: drop 4-arg limits; receive params ≥4 at `[rbp+16+8*i]`;
  emit_call places args ≥4 at `[rsp+32+(g-4)*8]`; frame outgoing
  = max(io_out, 8*(maxargs-4)); 16B alignment held by `frame&!15`.
  (Matches their `frame_fixed`/`io_out`/`max_stack_args` approach.)
- A2: `Type::Fn{ret,params}`; fn-ptr = `Ptr(Fn)`; `Expr::CallPtr`;
  targeted fn-ptr declarator `( * id [dims] )( ptypes )` (no full
  recursive-declarator rewrite); function-name decay via new
  `RipRef::Func` + `lea rax,[rip+d]`; indirect `call rax` (FF D0);
  pe.rs resolves `RipRef::Func` → `text_rva+offsets[name]`.

## Progress log
- [2026-05-17] Designed Phase A; RED confirmed 9/9.
- [2026-05-17] Collision discovered on first codegen.rs Edit.
  Verified no self-modification. STOOD DOWN. Journal rewritten to
  the honest record. Tasks updated. Committing journals only.

## RESOLUTION (2026-05-18) — Arthur: "Repo is yours. Push on."

The constraint was lifted. The concurrent engineer had completed
Phase A in-tree (impl in src/{ast,codegen,parser,pe}.rs, RED
suite tests/calling.rs, O2/O3 corpus, HLD, a code-review, and
apply-feedback: MINOR#1 fixed at root via Gen::named_fnptr,
MINOR#2 tracked) but left it UNCOMMITTED.

Taking ownership, I independently re-verified before committing:
- `cargo test`: lib 59 · calling **10/10** · differential 15
  (O2 cl + O3 bcc32, 3-way) · end_to_end **88/88** (O1, byte-
  identical) · pe_layout 2 · printf_format 5 — ALL GREEN.
- `cargo clippy --all-targets -D warnings` — clean.
- Read the full code-review (APPROVE-WITH-NITS) + HLD; impl
  matches HLD; findings honestly resolved/tracked.

Committed Phase A implementation atomically: **77d3872**
(src ast/codegen/parser/pe + tests/calling.rs + corpus
manyargs/funcptr, 7 files, +544/-23). Docs/plan/review commit
next. Then continuing to **Phase B** (HLD already drafted:
`wrk_docs/2026.05.18 - HLD - Phase B (virtual functions +
vtables).md`) per the plan of record.

My independent design (above) matched the shipped approach —
kept as a cross-check footnote; no rework needed.
