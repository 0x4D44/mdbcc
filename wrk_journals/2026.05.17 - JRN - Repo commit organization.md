# JRN — Repo commit organization (2026-05-17)

Task (Arthur): Work out what the uncommitted changes cover; decide
commit-vs-gitignore; group into sensible groupings and commit (incl.
wrk_docs / wrk_journals); enforce `YYYY.MM.DD - <desc>` doc naming;
push. Then (handed over for the night): continue autonomously, make
sensible decisions, journal as I go.

## Inventory of uncommitted changes

Tracked (modified):
- `src/ast.rs`        — **C++ only** (Record/Field, Type::Record,
  Type::Ref, Expr::Member/MethodCall/New/Delete, TU.records/defaults).
- `src/parser.rs`     — **C++ only** (grep: 0 printf hits).
- `src/codegen.rs`    — **C++ AND printf, entangled** (shared `use`,
  `Module`/`Overload`/`Gen`, `ntemps 8→16`). Prior journal explicitly:
  "cannot be cleanly separated by file — flag for Arthur".
- `tests/end_to_end.rs` — **C++ only** (+548 ln, cxx_* tests).
- `notes/scratchpad.md` — printf note + PE-blocker note.
- `wrk_journals/2026.05.17 - JRN - Oracle v1 implementation.md`
- `wrk_journals/2026.05.17 - JRN - PE dynamic section layout.md`

Untracked:
- `tests/printf_format.rs`            — **printf only** (O1 oracle, 5 tests).
- `tests/corpus/portable/printf_*.c`  — **printf only** (6 corpus files).

## Decisions

- **gitignore?** No new ignores needed. `.gitignore` already covers
  `target/`, `wrk_tools/`, `wrk_corpus/`, `*.tmp`, `*.log`. The
  untracked printf files are test oracles → must be committed (exactly
  the differential-oracle value CLAUDE.md prizes). No build artefacts
  among untracked.
- **Naming convention.** All dated docs already conform
  (`wrk_docs/`, `wrk_journals/`, `wrk_journals/reviews/`). Only
  `notes/scratchpad.md` + `notes/spike-oracles.md` don't — both are
  CLAUDE.md-mandated fixed-path working files, not dated designs.
  Asked Arthur → **keep `scratchpad.md` as-is** (exception). Not
  renaming `spike-oracles.md` (pre-existing, not in my diff).
- **codegen.rs entanglement.** Will NOT hunk-split it (prior author
  who wrote it says it can't be cleanly separated; non-interactive
  env; a bad split risks non-compiling commits = defeats atomicity).
  codegen.rs goes whole into the C++ commit; message discloses it
  also carries the entangled printf-format impl.

## Commit plan (each atomic: compiles + full suite green)

1. **C++ language support** — `src/ast.rs`, `src/parser.rs`,
   `src/codegen.rs`, `tests/end_to_end.rs`. Discloses codegen.rs also
   contains entangled printf-format work (per Oracle-v1 journal).
2. **printf format-specifier test oracle suite** — `tests/printf_format.rs`,
   `tests/corpus/portable/printf_*.c`. (Impl already in commit 1 via
   codegen.rs; this adds the 3-way O1/O2/O3 validation.)
3. **Engineering journals & scratchpad** — the two modified JRN files,
   `notes/scratchpad.md`, + this journal.

Pre-commit gate: `cargo test` exit 0 (end_to_end 88 / pe_layout 2 /
printf_format 5 / lib + differential green), `cargo clippy
--all-targets -D warnings` — running.

## Concurrent-work note
Mid-task, two untracked files appeared from another workstream:
`wrk_journals/2026.05.17 - JRN - Roadmap to OWL.md` and
`...PLAN - OWL Windows roadmap.md`. NOT part of the change set I
was asked to organize (they materialized after I started, from a
concurrent agent). Per CLAUDE.md multi-agent guidance I left them
UNTOUCHED & uncommitted — committing another engineer's in-flight
planning docs would be premature. Flagged for Arthur.

## Progress log
- [2026-05-17] Inventory + decisions done. `cargo test --no-run`
  exit 0; full `cargo test` exit 0; `cargo clippy --all-targets
  -D warnings` exit 0. Journal created.
- [2026-05-17] Commit 1 `0e061d6` — C++ subset (ast/parser/codegen/
  end_to_end, 4 files, +3117/-79). Message discloses codegen.rs
  also carries entangled printf impl (per Oracle-v1 journal).
- [2026-05-17] Commit 2 `dc74e83` — printf test oracle suite
  (printf_format.rs + 6 corpus .c, 7 files, +183).
- [2026-05-17] Commit 3 `d85b1d6` — journals + scratchpad (4
  files, +210). OWL files deliberately excluded.
- [2026-05-17] Pushed. `git fetch . feat/test-oracles-v1:main`
  (non-disruptive ff, working tree untouched) → main=d85b1d6.
  `git push origin main` (ecb82e3..d85b1d6 ff, no force) +
  `git push -u origin feat/test-oracles-v1` (new remote branch).
  All four refs (local/origin × main/branch) == d85b1d6. VERIFIED.

## ORIGINAL TASK COMPLETE
3 atomic commits, full suite green + clippy clean at the committed
state, pushed to origin/main (deliverable, per documented branch->
ff->main workflow) and origin/feat/test-oracles-v1 (backup). No
gitignore changes needed. Naming convention satisfied (scratchpad
exception confirmed by Arthur).

### Residual / flagged for Arthur
- `src/codegen.rs` committed whole in 0e061d6 — contains BOTH the
  C++ subset and the entangled printf-format impl (could not be
  cleanly file-split; prior author's explicit note). Disclosed in
  the commit message. No clean way to attribute per-feature.
- 2 untracked OWL files (`JRN - Roadmap to OWL.md`, `PLAN - OWL
  Windows roadmap.md`) from a concurrent workstream left UNTOUCHED
  & uncommitted (appeared mid-task; not mine to stage).
- `notes/scratchpad.md` "PE 4 KB blocker" note is STALE — the PE
  journal records that defect as FIXED (36056b4/2010507/7cf7390).
  Committed as-is (faithful to working-tree state); correcting
  stale self-notes is a separate judgement call (see autonomous
  continuation journal).

---
Continuing autonomously per Arthur's overnight handover. Further
work tracked in a separate JRN (this one covers only the commit/
push task, now closed).

## Autonomous continuation outcome
Picked the plan-of-record next item (Phase A: >4 args + function
pointers). On the first `src/codegen.rs` Edit, discovered a
**concurrent engineer is actively implementing the same Phase A**
(prologue + frame already done by them; they own the HLD + RED
suite). Per CLAUDE.md multi-agent rules I **stood down** rather
than collide — the correct decision, not a block. Zero self-edits
to shared code (the failed Edit was never applied; verified).
Full record + my independent cross-check design:
`wrk_journals/2026.05.17 - JRN - Phase A calling path.md`.
Net: original task done+pushed; no interference with concurrent
Phase A; journals committed separately (disjoint files).
