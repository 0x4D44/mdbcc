# 2026.06.07 — G8b: GDI32/USER32 import surface; railc closure 498 → 358; the whole remainder is Stage-3 library files

Eighth arc. Closed the dominant W5 closure bucket (task #13) — the
OWL→Win32 import surface — with an empirically-measured −140 drop, and
PROVED the import resolution complete for railc.

## G8b (c3309c9): the GDI32/USER32 surface OWL calls

OWL's library bodies call ~143 GDI32/USER32 APIs declared `extern "C"
WINAPI` in the windows headers; mdbcc C++-mangled them so they missed the
linker's `is_win32_import` table → unresolved. codegen's `gen_call` already
checks `is_win32_import(name)` before the mangled fallthrough — the gap was
table coverage. Added 143 entries (115 GDI32, 27 USER32, 1 KERNEL32) to
WIN32_IMPORTS + ALL to EXTENDED_ON_DEMAND (on-demand on BOTH targets ⇒ every
image not calling them — the 88 baselines, x64 goldens, non-OWL programs —
byte-identical). DLL mapping derived mechanically from which windows header
declares each.

EMPIRICAL (rebuild all 3 archives → relink railc): **closure 498 → 358
(−140)**. RUN-verified vs bcc32: `GetMenuItemCount(NULL)` → -1 both → 42.

## The closure is now PURELY library-file symbols

Re-scanned the 358: the only remaining single-segment mangled names are 9
OWL/RTL INTERNALS (CreateInstanceThunk, GetWindowPtr, HandleGlobalException,
SetCreationWindow, TVectorImpBase, fputs, strnewdup, to_upper) — **zero raw
Win32 APIs missed**. The other 349 are out-of-line members
(`@TApplication@$bctr…`, `@TDC@ExtTextOutA`, `@TFrameWindow@$bctr…`, etc.)
of the 58 OWL + CLASSLIB files that DON'T yet compile, plus iostream (42),
plus BIDS TVectorImpBase.

So every remaining unresolved symbol is defined by a library source file
mdbcc cannot yet compile. There is NO further linker/import work — the
mdar→mdlink pipeline + import resolution are complete. The entire gap is
now compiler-frontend: get those files compiling.

## Terminal path (unchanged order, now with no linker unknowns)

1. **Stage-3 data-bearing virtual bases** (task #10) — the dominant blocker:
   the TDialog/TFrameWindow/TWindow construction families, the iostream
   diamond, railc LAYOUT.CPP, DATEIO/TIMEIO. Un-blocks the bulk of the 349.
2. Smaller buckets: G3s2/#64 (DECLARE_STREAMER nested-tag collision),
   by-value class catch (DIALOG), the remaining singles.
3. The few RTL holdouts (fputs/to_upper/Sqrt + the iostream files).
4. `.res` linking (reuse golden railc.res; reader + .rsrc emitter).
5. full archives → mdlink + .res → run on WOW64 → diff golden (W5/W6).

## Net (day: 28 commits)

The W5 link infrastructure is DONE: archives build (mdar), members pull
symbol-driven, library duplicates fold, Win32 APIs resolve as imports. railc
links to a 358-symbol residue that is entirely the not-yet-compiling library
source — converting the goal's tail to a single front-end axis (Stage-3 +
small buckets) with every linker unknown eliminated and every step
empirically measurable by the relink closure count.
