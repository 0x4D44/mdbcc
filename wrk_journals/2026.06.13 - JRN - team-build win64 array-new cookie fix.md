# JRN — team-build: Win64 array-new/delete cookie fix (MDBCC-01 runtime wall)

**Date:** 2026-06-13
**HLD:** `wrk_docs/2026.06.13 - HLD - mdbcc Win64 array-new cookie fix.md` (Status: SIGNED-OFF)
**Lead:** Claude (team-build). User away after kickoff.

## Definition of done (HLD acceptance O1–O5)
- **O1** new differential matrix `new(alloc)C[n]; delete[]` (ctor+dtor; dtor-only; implicit dtor via member/base; large n;
  post-delete alloc+free) on BOTH -m32 and Win64 vs cl/bcc32 — currently fails Win64 (0xC0000374); must pass both after.
- **O2** symmetry: `*((size_t*)p-1)==n` for the allocator-placement class array.
- **O3** 18 `tests/cpp_new_array.rs` + S4 `oracle_s4_classlib.rs` stay green (both targets); full non-ignored suite green.
- **O4** `railc_source_slice.rs` Win64 smoke no longer `HeapCorruption`; then flip O3 arm (~:834) eprintln!→panic!.
- **O5** no i386 RailC regression.

## Design (one function: gen_placement_new_array, mirror gen_new_array's exact cookie protocol)
- Part 1: when `array_needs_cookie(ty)`, allocator form grows alloc by target_ptr_bytes(), stores count@block+0,
  returns block+cookie (= the gen_new_array layout gen_delete already expects). No delete-side change. Target-agnostic.
- Part 2 (separate commit): widen the class-ctor construction loop to 64-bit on Win64 (was 32-bit-only).

## Plan
1. VALIDATE (light, collapsed): read gen_new_array's exact cookie emission (reference), gen_placement_new_array current,
   array_needs_cookie, the existing allocator-placement test pattern. Confirm seams vs HEAD.
2. WI-1 tests-first: the O1 differential matrix + O2 symmetry.
3. WI-2 Part 1: the cookie in gen_placement_new_array (prefer a shared helper with gen_new_array).
4. WI-3 Part 2: 64-bit ctor-loop widening + its codegen test.
5. WI-4 verify: run the matrix + regressions (both targets) + the Win64 runtime smoke; flip O3 to hard gate.

---
## Phase 1 — VALIDATE ✅ (no drift)
Line numbers current: gen_placement_new_array:14540, gen_delete:14765, array_needs_cookie:15223 (ctor OR dtor),
gen_new_array:15268, gen_new_array_i386:15699. Read the reference: gen_new_array Win64 cookie path (15364-15440) —
bytes=n*elem_size+8 (negative/overflow ud2 traps), HeapAlloc, store *(u64*)block=n (15416-15435), payload=block+8
(15439), construct, return payload. Current gen_placement_new_array is cookie-less in ALL forms; its class-ctor loop
(14639-14664) is 32-bit-only (Part 2). Allocator form allocates via `operator new[](count*elem_size, alloc)` (Expr at
14578-14590), so the cookie wraps that call (size += cookie; store count@block; payload=block+cookie). The dtor-only
class currently mis-routes through the cookie-less ctor.is_none() branch (14593) → must also cookie. No fundamental
conflict → proceed (no PARK). Tests: tests/cpp_new_array.rs (18, Win64), tests/oracle_s4_classlib.rs (S4 trivial).

## Phase 2/3 — PLAN + IMPLEMENT ✅
One implementer agent (tests-first → Part 1 cookie → Part 2 ctor-loop widening, iterating to green). Result:
- **Part 1**: gen_placement_new_array allocator form, gated `needs_cookie = !is_ptr && !is_array && array_needs_cookie(ty)`;
  size += cookie_size (target_ptr_bytes); store count@block (u64 Win64 / u32 i386, mirroring gen_new_array); payload=block+cookie;
  dtor-only class now cookie'd (no longer mis-routed); count spilled once; `// allocation_base = payload - cookie_size` note.
- **Part 2**: ctor loop target-branched — Win64 REX.W (no >4GB truncation); i386 32-bit byte-identical.
- Untouched: raw-buffer placement, trivial elements, scalar, gen_delete/gen_new_array/gen_global_alloc_op.
- **New** `tests/cpp_placement_new_array_cookie.rs` (6 tests): new(alloc)C[n];delete[] roundtrips (ctor+dtor, N=64, dtor-only,
  implicit-dtor-via-member, trivial control, Part-2 REX.W lock) on BOTH targets; O2 symmetry; post-delete heap-validate. Pre-fix
  failed Win64 (0xC0000374); post-fix 6/6 green both targets.
- Lead re-verified: cpp_new_array 36/0/1ign + new 6/6 green (combined tree).
- **Deviation (D-impl):** Part 1+2 landed in ONE commit (same function; the HLD's two-commit split was a review nicety) — `6055544`.

## Concurrency handling (multi-agent)
A concurrent engineer's unrelated 4-byte-float fix has UNCOMMITTED hunks in src/codegen.rs (`@@ -35/-3629/-10916/-10992/-11024`)
+ src/codegen/abi.rs + tests/floats.rs + notes/float-global-track-bug.md. My hunks are disjoint (`@@ -14493/-14538/-14567/-14587`,
gen_placement_new_array). Per multi-agent policy I committed ONLY my hunks via `git apply --cached` of an extracted patch +
`git add` of the new test/journal — the float work remains unstaged/untouched in the working tree. Did not edit/revert their files.

## Phase 4 — VERIFY (O4) + REVIEW ✅ — 🎉 WALL CLEARED
**O4 runtime smoke (release rebuild with the cookie fix → recompile/link the Win64 slice → smoke):**
`railc=21 owl=65 streams=48 bids=11 rtl=207`, exe 12,389,888 B; O0/O1/O2 green; **`O3: a visible top-level window
appeared — wall 1 CLEARED`** (913s incl. the release codegen rebuild). Not merely "no 0xC0000374" — an ACTUAL native
Win64 top-level RailC window. The cookie fix cleared the startup heap corruption; the OWL static-dispatcher WNDPROC
patch (committed earlier) made the window path pointer-safe — both were needed and now compose.

**O3 flipped to a HARD GATE** (`report_win64_runtime_smoke` → `assert_win64_runtime_smoke`; HeapCorruption arm
eprintln!→panic!) — commit `0f3ea54`. The just-passed run (WindowAppeared) confirms the gate is green; the panic! arm
locks against a 0xC0000374 regression.

**REVIEW (proportionate, collapsed):** the fix mirrors the proven `gen_new_array` reference, is isolated to one
function, and is empirically verified end-to-end (the new differential matrix on both targets + all regressions green
+ a real window). No heavy review workflow warranted.

## Final status — team-build COMPLETE ✅ (MDBCC-01 runtime wall DOWN)
- **Shipped & green:** the Win64 array-new/delete cookie fix (`gen_placement_new_array`, Part 1 cookie + Part 2 REX.W
  ctor loop) — commit `6055544`; the new `tests/cpp_placement_new_array_cookie.rs`; the O3 hard gate — `0f3ea54`.
- **Acceptance O1–O5 all met:** O1 new matrix (both targets) ✅; O2 symmetry ✅; O3 regressions green ✅; **O4 — Win64
  RailC opens a real window** ✅; O5 i386 stable (i386_run 263 + byte-identity) ✅.
- **Concurrency:** committed only my disjoint `gen_placement_new_array` hunks via `git apply --cached`; the concurrent
  engineer's 4-byte-float work (`codegen.rs`/`abi.rs`/`floats.rs`) remains unstaged and untouched.
- **Next frontier (expected, not a regression):** wall 2 — the deferred int32 OWL dispatch layer (`SIGNATUR.H`
  `I32_U_I32_Sig`, `DISPATCH.CPP`) and the `DWL_USER` dialog path — may surface on deeper interaction; the window
  rendering proves the bootstrap + startup are sound. A separate, smaller frontier when/if demanded.
