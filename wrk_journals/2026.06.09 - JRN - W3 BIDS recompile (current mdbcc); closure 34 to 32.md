# 2026.06.09 — W3 BIDS/CLASSLIB recompiled with current mdbcc; railc closure 34 → 32

Same proven deliverable-refresh as the W2 RTL recompile. The `wrk_oracle/bids_objs`
(19 CLASSLIB objects → `mdbids.lib`) were built by an older mdbcc; recompiled all
19 with the current toolchain.

## Recipe

```
bcc -c -m32 -D__WIN32__ -I C:/tmp/bc45/INCLUDE  C:/tmp/bc45/SOURCE/CLASSLIB/<T>.CPP
```

All 19 recompile cleanly (CASTABLE, CLASMAIN, DATE*, EXBASE, GEOMETRY, GEOMSTRM,
HEAPSEL, LOCALE*, MEMMGR, OBJSTRM, …). Rebuilt `mdbids.lib`.

## Effect

- railc closure **34 → 32**:
  - `GetExitCodeThread` — the CLASSLIB `TThread` caller now emits `__imp_…` (the
    import was added to WIN32_IMPORTS in the prior I/O-shim commit), resolving it.
  - `TVectorImpBase$S909$S887::Resize` — instantiates correctly under current
    mdbcc (the G33 comparison-deduction + earlier fixes let the BIDS vector
    template body codegen rather than defer).

(`bids_objs`/`mdbids.lib` are gitignored deliverables — no tracked source change.)

## State

Session arc: railc closure 343 → … → 36 → 34 → **32**. Remaining 32:
- **Stage-3** LAYOUT.CPP fstream (~13) — the critical-path major feature.
- **RTL parity residue** (~8) — `_lrand` (64-bit LCG), `__realcvt`/`__nextreal`
  (float fmt — hard), `min`, `@strnewdup`, `xerror::MakeString`, `readVersion`,
  `filebuf::seekoff`.
- **DDVT `*_Sig`** (5) — pointer-to-member template-arg deduction (needs `T::*`
  Type support).
- **MISC OWL** (3) — `TWindow::Register` (S4.2g defer on `ToBool(::GetClassInfo())`
  — a function-CALL return type the monomorphizer's `type_of_expr` can't yet
  type), `TAppMutex::NotWIN32s`, `TMutex::operator=`.
- **Win32 / C0** (3) — `@IsMenu` (USER32 API C++-mangled instead of imported),
  `_hInstance` (C0 startup global).
