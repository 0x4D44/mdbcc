# 2026.06.19 - JRN - compiler quality map seed

Goal: start `2026.06.19 - GOAL - compiler quality and oracle expansion.md`.

Worktree: `C:\worktrees\mdbcc\compiler-quality-oracles` on `task/compiler-quality-oracles`.

## Work Done

- Added `tests/quality_map.rs`, a deterministic Q0/Q1 report test:
  - prints the major oracle inventory;
  - calls out duplicated status/skip classifications;
  - separates soft/partial skips from success;
  - prints a top-10 ranked compiler backlog;
  - keeps known O12 reference gaps out of the mdbcc work queue.
- Expanded `tests/owl_examples_product.rs` from two hand-written ignored tests to an 11-entry stock OWL matrix:
  - existing BUTTON and INSTANCE green checks remain;
  - STATIC, HELLO, GROUPBOX, EDIT, GAUGE, LISTBOX, COMBOBOX, SLIDER, and POPUP are represented with expected phase classifications;
  - the ignored matrix reports observed phase classifications and repro commands;
  - missing BC4.52 source/libs/desktop are explicit environment skips.
- Added this Q0 inventory doc for a quick human read.
- Added `tests/abi_torture.rs`, a generated Q5 ABI/calling-convention matrix:
  - 10 deterministic named cases across Win64 and i386;
  - covers out-of-line static members, Win64 integer varargs after a double,
    stack arguments, record by-value args, record returns, virtual dispatch,
    and i386 derived-to-base reference adjustment;
  - each runtime failure reports the target and ABI feature class.
- Updated the quality map inventory/backlog so the ABI suite is represented as
  an active oracle and its remaining work is the external differential/reducer
  extension, not the initial generator.
- Added `tests/structural_invariants.rs`, a generated Q6 object-structure gate:
  - compiles high-risk Win64/i386 C++ objects;
  - checks relocation symbol/offset bounds, section-reference validity,
    duplicate strong externals, section alignment flags, section-definition aux
    records, vtable/typeinfo `.rdata` layout, Win64/i386 call-frame byte
    patterns, and Win64 `.pdata`/`.xdata` unwind shape;
  - updates the quality map so the remaining structural backlog is deeper
    linked-image coverage.
- Added `tests/external_corpus_adapter.rs`, a Q3 external corpus adapter:
  - records a 20-case LLVM/GCC torture execute manifest without vendoring
    third-party sources;
  - the ignored runner uses `MDBCC_LLVM_TEST_SUITE` or
    `wrk_oracle/external_c/llvm-test-suite`, skips loudly when absent, excludes
    non-UTF8 corpus bytes at the boundary, and falls back to the GCC torture
    exit-0 hand oracle when MSVC rejects old-C fixtures;
  - a temp-checkout probe compared 19 UTF-8 fixtures: 15 passed, 1 was excluded
    for raw source bytes, and 4 were mdbcc reds;
  - committed the reduced ignored regression
    `gcc_torture_reduced_implicit_int_function_definition_regression`.
- Added `tests/fuzz_reduction_path.rs`, a Q4 deterministic fuzz/reduction
  process proof:
  - records mini-fuzzer seed `0x4d42cc0000000001` and its command line;
  - keeps a default-on pure reducer determinism check;
  - keeps the real mdbcc/reference generate-diff-reduce path ignored because it
    intentionally targets the current C89 implicit-int red;
  - reduces the generated candidate to the stable regression text
    `main(){return 0;}`.
- Updated the quality map with map-driven outcomes so the first report records
  four concrete fixes/deferrals and their before/after evidence.

## Validation

- `rustfmt --check tests\owl_examples_product.rs tests\quality_map.rs`
- `mdtimeout 240 -- cargo test --no-run --test quality_map --test owl_examples_product`
- `mdtimeout 120 -- cargo test -q --test quality_map -- --nocapture`
- `mdtimeout 120 -- cargo test -q --test owl_examples_product owl_product_matrix_inventory_has_q2_shape -- --nocapture`
- `mdtimeout 120 -- cargo test -q --test owl_examples_product owl_stock_product_matrix_win64 -- --ignored --nocapture --test-threads=1`

The ignored OWL matrix is environment-skipped in this worktree because `wrk_oracle` is gitignored and not present here.

Additional Q5 validation:

- `mdtimeout 240 -- cargo test --no-run --test abi_torture`
- `mdtimeout 120 -- cargo test -q --test abi_torture -- --nocapture`

Additional Q6 validation:

- `mdtimeout 240 -- cargo test --no-run --test structural_invariants`
- `mdtimeout 120 -- cargo test -q --test structural_invariants -- --nocapture`
- Re-ran after the call-frame invariant extension with the same two commands.

Additional Q3 validation:

- `mdtimeout 240 -- cargo test --no-run --test external_corpus_adapter`
- `mdtimeout 120 -- cargo test -q --test external_corpus_adapter external_corpus_manifest_has_q3_shape -- --nocapture`
- `mdtimeout 120 -- cargo test -q --test external_corpus_adapter llvm_gcc_torture_external_corpus_pilot_msvc -- --ignored --nocapture`
- With `MDBCC_LLVM_TEST_SUITE=target/external_probe/llvm-test-suite`, the ignored
  pilot intentionally failed with four mdbcc reds after comparing the temporary
  external checkout.

Additional Q4 validation:

- `mdtimeout 240 -- cargo test --no-run --test fuzz_reduction_path --test quality_map`
- `mdtimeout 120 -- cargo test -q --test fuzz_reduction_path fuzz_seed_and_reducer_are_deterministic -- --nocapture`
- `mdtimeout 120 -- cargo test -q --test fuzz_reduction_path fuzz_generate_diff_reduce_regresses_c89_implicit_int_main -- --ignored --nocapture`
- `mdtimeout 120 -- cargo test -q --test quality_map -- --nocapture`

Final consolidated validation:

- `rustfmt --edition 2024 --check tests\quality_map.rs tests\owl_examples_product.rs tests\abi_torture.rs tests\structural_invariants.rs tests\external_corpus_adapter.rs tests\fuzz_reduction_path.rs`
- `mdtimeout 240 -- cargo test --no-run --test quality_map --test owl_examples_product --test abi_torture --test structural_invariants --test external_corpus_adapter --test fuzz_reduction_path`
- `mdtimeout 240 -- cargo test -q --test quality_map --test owl_examples_product --test abi_torture --test structural_invariants --test external_corpus_adapter --test fuzz_reduction_path -- --nocapture`

## Notes

- I accidentally applied the first patch from the main checkout. I moved the edits into the task worktree and restored the main checkout; it now only has the unrelated pre-existing untracked B-08 HLD doc.
- `cargo fmt --check` is not currently useful as a whole-repo gate: it reports pre-existing formatting drift in many untouched files. I used `rustfmt --check` on the files touched here instead.
