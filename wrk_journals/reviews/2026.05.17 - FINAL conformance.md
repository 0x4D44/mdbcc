# FINAL plan-vs-implementation conformance audit — PE dynamic section layout

**OVERALL: 100% CONFORMANT**

Scope: branch `feat/test-oracles-v1`, commits `36056b4` (S1+S2) + `2010507`
(S3). Independent auditor; verified by reading committed code (read-only
`git show`/`grep`/`Read`), not by trusting the journal narrative.

Counts: **PASS 31 / PARTIAL 0 / GAP 0** (all plan §1–§6 checkable items +
all 6 recorded supervisor deviations).

---

## §1 — Defect anatomy: every propagation point now layout-derived

| Item | Verdict | Evidence (src/pe.rs unless noted) |
|---|---|---|
| 4 fixed RVA consts removed | PASS | `grep TEXT_RVA\|IDATA_RVA\|RDATA_RVA\|DATA_RVA` → **no matches**; diff `36056b4` deletes the 4 `const … = 0x1000..0x4000` lines (lines 24-27 of old file). |
| No lingering hard-coded section RVA anywhere | PASS | `grep 0x1000..0x5000` → only `SECT_ALIGN` const (32), doc comments, stack/heap commit `0x100000/0x1000` (430-433, unrelated, untouched), section *characteristics* `0x4000_0040` (a flag, not an RVA), and test asserts. No production RVA literal remains. |
| idata internal thunks layout-derived | PASS | `build_idata(idata_rva)` (92); `let rva = |off| idata_rva + off;` (118) — was `IDATA_RVA + off`. ILT/IAT/desc/name all via `rva(...)`. |
| rdata RIP-target RVAs layout-derived | PASS | `build_rdata` returns *offsets* (177-181); caller rebases `o + rdata_rva` (361-364). |
| .data ABSOLUTE pointer-to-string value | PASS | `build_data(.., rdata_rva)`; `let str_rva = rdata_rva + rdata.len(); let abs = IMAGE_BASE + str_rva` (198-200) — was `RDATA_RVA + …`. Globals offset-based (191-204), caller rebases `o + data_rva` (365). |
| IAT / Import data directories | PASS | `dirs[1]=(idata.import_dir_rva,…); dirs[12]=(idata.iat_rva,…)` (439-440); both fields set from `rva(desc_off)` / `rva(iat_off)` inside `build_idata` (159-162). |
| All 4 section-header VAs | PASS | `write_section_header(.., text_rva/idata_rva/rdata_rva/data_rva, ..)` (447-462) — diff replaces all 4 `*_RVA` consts with computed vars. |
| AddressOfEntryPoint / BaseOfCode | PASS | `b.u32(text_rva)` ×2 (413-414) — was `TEXT_RVA`. |
| SizeOfImage from real layout | PASS | `size_of_image = align_up(data_rva + data_vsize, SECT_ALIGN)` (390) — was `align_up(DATA_RVA + data_vsize, …)`. |
| build_text stub disp / next_rva | PASS | `disp = exit_rva - (text_rva + 0x11)` (250); `next_rva = text_rva + site + 4` (286) — both were `TEXT_RVA`. |
| Module doc comment updated | PASS | Lines 1-25 rewritten to "four sections … Dynamic layout … align_up(prev_end)" with the new offset table. |

## §2 — Target design

| Item | Verdict | Evidence |
|---|---|---|
| Section order unchanged (.text,.idata,.rdata,.data) | PASS | Section-header emission order 447-462 and file-data order 465-472 both `.text→.idata→.rdata→.data`; `NumberOfSections=4` (399). |
| Linear no-circularity order as written | PASS | `text_vsize` pre-pass (338-339) → `text_rva=align_up(HEADERS_SIZE,SECT_ALIGN)` (341) → `idata_rva` (342) → `build_idata` (343) → `rdata_rva` (346) → `build_rdata` (347) → `build_data(.., rdata_rva)` (350) → empty guards (351-356) → `data_rva` from *final* rdata (359) → rebase (361-365) → `build_text(.., text_rva)` (367) → `size_of_image` (390). Strictly linear; no back-reference. |
| Drift guard ALWAYS-ON `return Err` (not debug_assert) | PASS | 371-377: `if text.len() as u32 != text_vsize { return Err(CodegenError(format!(…both values…))) }`. No `debug_assert` form of this check anywhere (the remaining `debug_assert_eq!` at 253/394/141 are pre-existing unrelated sanity asserts, not the pre-pass↔emit reconciliation). Matches deviation #4. |
| file-offset / SizeOfRawData / FILE_ALIGN / headers untouched | PASS | `FILE_ALIGN=0x200`, `HEADERS_SIZE=0x400`, `*_raw=align_up(*_vsize,FILE_ALIGN)`, `*_file_off` chain (381-389), `SizeOfHeaders=HEADERS_SIZE` (426) — unchanged vs old file in diff (only `DATA_RVA→data_rva` on the SoI line). DOS/NT header writers unchanged. |
| align_up unchanged | PASS | `fn align_up(v,a){ v.div_ceil(a)*a }` (38-40) — not in diff; `align_up(0x2000,0x1000)==0x2000` holds (div_ceil). |
| No Layout struct (deviation #3) | PASS | `build_text` takes 5 scalar args; `#[allow(clippy::too_many_arguments)]` only on `write_section_header` (pre-existing). 5 < clippy's 7 limit — smallest change, matches deviation #3. |
| empty-section push(0) guard placement | PASS | Guards (351-356) run AFTER `build_rdata`/`build_data` and BEFORE `data_rva` (359) computes off `rdata.len()` — preserved/correct ordering. |
| debug_assert text.len()==text_vsize "add" upgraded | PASS | Plan said add `debug_assert`; deviation #4 upgrades to always-on Err — strictly stronger, recorded, sound. |

## §3 — Staged plan / commits

| Item | Verdict | Evidence (`git show --stat`) |
|---|---|---|
| Both commits exist | PASS | `36056b4` "Fix PE writer: dynamic section layout…" + `2010507` "Add PE layout exact-SECT_ALIGN-boundary regression test" on HEAD. |
| S1+S2 combined GREEN | PASS | `36056b4`: 6 files +923/-41 = src/pe.rs (prod fix + RED test flipped green) + tests/pe_layout.rs + bigcode.c + journal + 2 review artifacts. One atomic green commit (deviation #2). |
| S3 separate | PASS | `2010507`: 3 files +453/-1 = src/pe.rs (test-only), journal, S3 review artifact. |

## §4 — Test / oracle strategy

| Item | Verdict | Evidence |
|---|---|---|
| O1 asserts non-loading PE hard-fails | PASS | `tests/pe_layout.rs:40` `Command::new(&tmp.0).output().expect("launch")` — an unloadable exe (the defect) panics here = RED. Journal records empirically-observed RED (Win32 err 193). |
| O1 asserts exact Rust-computed stdout+exit | PASS | `pe_layout.rs:99` `expected_stdout = format!("{TAG}{sum}\n")`, `sum = (0..NFUNC).map(fval).sum()` (88) computed in Rust; `assert_eq!(stdout,…)` + `assert_eq!(exit, expected_exit)` (103-110). |
| >8 KB .text | PASS | `NFUNC=800` non-trivial functions (50-51, 73-79); journal+commit msg state ≈132 KB .text. Far exceeds the 4 KB defect ceiling. |
| string + imported call + .data abs-pointer AFTER big .text | PASS | `char *tag="T:"` global + 800 fns then `main`; `printf("%s%d\n", tag, s)` (74-86): `.idata` printf import + `.rdata` format string + `.data` abs-ptr `tag` deref via `%s`, all emitted after the bloated `.text`. Codegen path real: `RipRef::Data` (codegen.rs:137,723 `lea rax,[rip+g]`, no const-fold) → pe.rs `ptr_str` abs (197-201). |
| bigcode.c strict-C89, portable, no `oracle: skip` | PASS | `tests/corpus/portable/bigcode.c`: C89 decls, int-only arith bounded `%1000`, `char*`+`%s` (ptr/int-size-independent). `grep "oracle: skip"` → **no directive in bigcode.c** (only unrelated dialect files). |
| No HashMap/HashSet ITERATION in sizing/emission path | PASS | `text_vsize` over `module.funcs.iter()` (Vec, ordered, 339); rebases iterate Vecs (361-365); HashMaps `offsets`/`iat_slot` are insert + point-lookup only, never iterated to produce bytes. `build_idata`/`rdata`/`data` iterate `IMPORTS`/Vecs. Determinism preserved. |
| Differential coverage of bigcode.c | PASS | `corpus_files("portable")` (support/mod.rs:564, sorted by path, **no size filter**) feeds O2 cl-behavioural (differential.rs:136), O3 acceptance (165), O3 Win32-behavioural (207, bigcode.c has no skip so active), and `mdbcc_determinism` double-compile byte-equality (264-279). |

## §5 — Risks & edge cases

| Risk | Verdict | Disposition |
|---|---|---|
| exact-SECT_ALIGN size / .text exactly 0x1000 | PASS | S3 unit test `exact_sect_align_boundary_no_spurious_page` (614-675): synthetic Module, `text_vsize ∈ {0x1000,0x2000}`, self-guard `text_vsize%SECT_ALIGN==0`, align_up-free anchor `idata_rva==text_rva+text_vsize`, overlap guard, SoI. Teeth empirically proven in journal (buggy align_up → fail). |
| large .data after large .text (abs ptr subtle) | PASS | Covered inside S1+S2 (deviation #5): `tag` .data abs-ptr deref'd via `%s` after ≈132 KB .text in pe_layout.rs + bigcode.c. **See deviation #5 below — confirmed genuinely covered, not lost.** |
| empty/degenerate sections (push(0) before sizes) | PASS | Guards 351-356 before `data_rva` (359); exercised by every small-program test (no globals/strings) incl. lib `pe::tests`. |
| file-align vs section-align independence | PASS | `*_raw=align_up(_,FILE_ALIGN)` independent of `*_rva=align_up(_,SECT_ALIGN)`; lib test `file_is_section_padded` (554-559). |
| BaseOfCode/SizeOfCode/SizeOfInitializedData | PASS | `BaseOfCode=text_rva` (414), `SizeOfCode=text_raw` (410), `SizeOfInitializedData=idata_raw+rdata_raw+data_raw` (411) all layout-derived; small-prog byte-identity (journal: text=0x1000…SoI=0x5000) regression-proves them. |
| pre-pass == build_text drift guard | PASS | Always-on Err (371-377); §2 above. |
| u32 / no >4 GiB (match existing unchecked style) | PASS | All sizes `u32`, `as u32` casts match pre-existing unchecked style; no overflow-check churn (out-of-scope §6, respected). |
| DOS/NT header fixed sizes unchanged | PASS | `write_dos_header` + COFF/optional-header constants not in diff except the targeted RVA/SoI lines. |
| debug_assert vs release | PASS | Resolved by deviation #4 (always-on Err) — the one correctness-critical invariant is release-safe. |
| clippy::too_many_arguments | PASS | 5-arg `build_text`, no new allow; clippy `-D warnings` clean (journal, both stages). |

No risk is silently uncovered; each is either tested or consciously resolved
via a recorded deviation.

## §6 — Out of scope / non-goals

| Item | Verdict | Evidence |
|---|---|---|
| Section count/order/flags unchanged | PASS | `NumberOfSections=4` (399); order .text/.idata/.rdata/.data (447-472); characteristics `0x6000_0020 / 0xC000_0040 / 0x4000_0040 / 0xC000_0040` (449-461) unchanged in diff. |
| Entry stub conceptually unchanged | PASS | `STUB_LEN=0x12`, stub bytes (244-252) unchanged; only `TEXT_RVA→text_rva` substitution. |
| Import set: IMPORTS 3→6 is PRE-EXISTING unrelated, not depended on | PASS | Diff shows `IMPORTS [&str;3]→[&str;6]` (+GetProcessHeap/HeapAlloc/HeapFree) + 1 doc-ws line — disclosed in commit `36056b4` body and journal tripwire. The layout fix is **size/index-agnostic**: `n=IMPORTS.len()` drives all idata math; nothing in the layout fix hard-codes 3 or 6. Out of audit scope; **not depended upon** by the fix. |
| Module/compile_to_pe/codegen public API unchanged | PASS | `pub fn write_pe` sig unchanged (329); `compile_to_pe` (compile.rs:55) untouched by both commits; `pub struct Module` / `CompiledFn` / `GlobalData` / `pub enum RipRef` (codegen.rs:132-167) untouched by both commits. `build_*` are private. |
| No .reloc/ASLR added | PASS | `DllCharacteristics=0` (429); abs-ptr mechanism kept & rebased, not relocated; no `.reloc` section. |
| No new crate deps | PASS | `Cargo.toml` `[dependencies]` block empty. |
| No formatting churn beyond touched lines + doc | PASS | `36056b4` pe.rs = 136 changed lines, all on the layout/doc/IMPORTS(disclosed) path; `2010507` pe.rs = test module only. No unrelated reflow. |

## Recorded supervisor deviations 1–6

| # | Verdict | Assessment |
|---|---|---|
| 1. O1 in NEW tests/pe_layout.rs (not end_to_end.rs) | PASS | File exists, in-process compile+run+assert mirroring printf_format pattern; O1 authority preserved (exact stdout+exit, real launch). Sound: keeps commit atomic vs 548 lines pre-existing WIP. Not a gap. |
| 2. S1+S2 = `36056b4`; S3 = `2010507` | PASS | Confirmed via `git show --stat`. Every commit compiles+green (journal suite tallies). Sound. |
| 3. No Layout struct | PASS | 5 args < clippy 7-limit, no `#[allow]` added; smallest change honored. Sound. |
| 4. debug_assert → always-on `return Err` | PASS | Verified at 371-377 (no debug_assert variant remains for this invariant; existing `CodegenError` type; both values in message). Strictly stronger than the plan's `debug_assert`; release-safe. Sound. |
| 5. big-.data-after-big-.text covered IN S1+S2; S3 reduced to exact-SECT_ALIGN | **PASS — explicitly confirmed** | pe_layout.rs deref's `.data` abs-ptr `tag` via `%s` after ≈132 KB `.text` with exact-bytes assert (65-110); bigcode.c does the same for O2/O3 differential (15,147). Codegen trace is real (`RipRef::Data`→`lea rip+g`, no const-fold; stale `rdata_rva` ⇒ wrong addr ⇒ fault/garbage ⇒ assert fails — cannot pass spuriously). S3 is correctly the lone exact-SECT_ALIGN test (614-675). The subtle abs-ptr concern is genuinely covered inside S1+S2, **not lost**. |
| 6. bigcode.c given a .data string-pointer global | PASS | `char *tag="T:"` + `printf("%s…",tag,…)` (bigcode.c:15,147); portable char*/%s gives O2(cl)+O3(bcc32)+determinism differential on the abs-ptr-after-big-text path. Sound. |

---

### Conclusion
Every checkable plan item (§1–§6) is implemented exactly as specified or
covered by a sound, recorded supervisor deviation. All 6 deviations are
justified, recorded, and not gaps-in-disguise — in particular deviation #5
(big-data-after-big-text covered in S1+S2) is independently confirmed to
hold with real, teeth-bearing coverage. The pre-existing IMPORTS 3→6 change
is disclosed, out of scope, and not depended upon by the layout fix.

**OVERALL: 100% CONFORMANT — 0 gaps.**
