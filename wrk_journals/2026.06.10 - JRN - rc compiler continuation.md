# 2026.06.10 - JRN - rc compiler continuation

## 2026-06-10 16:35

Arthur asked me to read the railc self-host handover and continue the work. The first full-file read of the handover timed out despite the file being small, so I switched to bounded reads. I read the handover, the active goal doc, the closure campaign journal, and the RC inventory report.

Current understanding: W0-W4 are done, W5 is link-complete without resources, W6 reaches railc application code and fails because resources are missing. The next blocking work is RC compiler task #10, with gaps 1-4 already landed and gaps 5-10 remaining. I am starting with gap 5: named resource IDs, style expressions/symbols, ICON control shorthand, and the shorthand-style merge rule.

Emotional temperature: focused, slightly annoyed by the PowerShell timeout because it wasted a few minutes, but the handover is clear and the path is concrete.

## 2026-06-10 17:29

Implemented the first focused slice of RC gap 5:
- Added `ResId::{Ord, Name}` for top-level MENU/ACCELERATORS/DIALOG resources.
- Added `ICON` as a dialog-control shorthand keyword.
- Replaced integer-only STYLE parsing with a small `|` expression evaluator over the RAILC-required built-in symbols (`WS_*`, `DS_MODALFRAME`, `BS_*`, `SS_*`).
- Fixed explicit shorthand-control style merging so it no longer forces `WS_CHILD|WS_TABSTOP`; it preserves the user style and ORs only class-specific low bits such as `BS_DEFPUSHBUTTON` or `SS_ICON`.
- Fixed wrapped negative integer narrowing so `-1` dialog control IDs parse back to `i16` and emit as `0xffff`.
- Taught the `.res` writer to emit string-form resource names in headers. The PE `.rsrc` collector remains ordinal-only and fails loudly for named resources until gap 10.

Added parser and `.res` tests for named menu/dialog IDs, style expressions, `ICON` shorthand default/explicit styles, and named menu `.res` headers. The named menu test was checked against the local Borland Resource Compiler 5.40 oracle.

Verification:
- `cargo test -q --test rc_parse`: pass, 36 tests.
- `cargo test -q --test rc_res`: pass, 29 tests, brc32 oracle present.
- `cargo test -q --test pe_rsrc`: pass, 9 tests.
- Targeted `rustfmt --edition 2024 --check` on touched files: pass.
- `cargo clippy -- -D warnings`: pass.
- `cargo test -q`: pass.
- `cargo fmt -- --check`: fails repo-wide on unrelated pre-existing formatting drift. I did not apply a global formatter rewrite because it would swamp the RC change.

I accidentally triggered whole-tree formatting once, then cleaned the generated churn and refreshed the racy Git index. Final content diff is scoped to six intended files plus this journal.

Emotional temperature: steady. The only frustration was Git/rustfmt noise; the actual RC change is mechanically clean and well covered.

## 2026-06-10 17:35

Continued the RC compiler work with gap 7:
- Added top-level `ICON`, `BITMAP`, and `RCDATA` resources to the RC AST.
- Taught `compile_file` to keep the `.rc` directory so file-backed binary resources resolve relative to their source file.
- Implemented binary payload parsing from either a quoted file path or inline raw-hex `BEGIN`/`END` data.
- Cooked single-image `.ico` files into RT_ICON image bytes plus RT_GROUP_ICON metadata with source-order icon ordinals.
- Cooked `.bmp` files by stripping the BITMAPFILEHEADER and preserving the Win32 RT_BITMAP DIB payload.
- Emitted RT_ICON, RT_GROUP_ICON, RT_BITMAP, and RT_RCDATA records from the `.res` writer, including string-form resource names.

Real RailC probe: `rc::compile_file(C:\language\railc\RESOURCE\RAILC.RC, {})` now gets past the menus, dialogs, icons, bitmaps, RCDATA, and string tables. It stops at `1333:1` on `VERSIONINFO`, exactly the next documented RC gap.

Verification:
- `cargo test -q --test rc_parse`: pass, 37 tests.
- `cargo test -q --test rc_res`: pass, 30 tests.
- `cargo test -q --test pe_rsrc`: pass, 9 tests.
- `cargo clippy -- -D warnings`: pass.
- `cargo test -q`: pass.
- Targeted `rustfmt --edition 2024 --check` on the touched parser/writer/test files: pass.
- `cargo fmt -- --check`: still fails repo-wide on unrelated pre-existing formatting drift. I left it alone rather than flooding the tree with formatter churn.
- `git diff --check`: pass.

Remaining risks: the `.res` writer still uses the current tier ordering rather than the BC4.5 source-order profile, PE `.rsrc` collection still rejects named top-level resources until gap 10, ICO support is deliberately single-image because that is enough for the current RailC assets, and `VERSIONINFO` is now the next parser blocker.

Emotional temperature: good momentum. The work feels less speculative now because the real `RAILC.RC` probe gives a clean next failure instead of a vague resource-wall failure. I am still irritated by whole-tree formatter drift, but it is contained.

## 2026-06-10 17:49

Pushed the next RC increment, gap 8:
- Added `VERSIONINFO` to the lexer, AST, parser, and `.res` writer.
- Parsed fixed fields (`FILEVERSION`, `PRODUCTVERSION`, `FILEFLAGSMASK`, `FILEFLAGS`, `FILEOS`, `FILETYPE`, `FILESUBTYPE`) including `L`-suffixed integers and four-part version quads.
- Parsed recursive `BLOCK` / `VALUE` trees and preserved embedded NULs in VALUE strings exactly as written.
- Encoded RT_VERSION as a standard `VS_VERSION_INFO` tree with a 52-byte `VS_FIXEDFILEINFO`, Borland-compatible node `wLength` / `wValueLength` / `wType`, and DWORD alignment rules measured against brc32 5.40.
- Added parser and `.res` tests for VERSIONINFO; the `.res` test compares against the local Borland oracle and has a hand-built fallback.

Real RailC probe: `rc::compile_file(C:\language\railc\RESOURCE\RAILC.RC, {})` now parses the full file successfully (`ok resources=21`). Generating a scratch `.res` and comparing just the RailC RT_VERSION payload against `wrk_oracle\railc_golden\railc.res` showed `payload_equal=True length=898`. The RT_VERSION header still differs in language (`0x0809` current writer default vs `0x0000` golden), which belongs to the already-listed BC4.5 writer profile gap.

Verification after the combined gap 7 + gap 8 work:
- `cargo test -q --test rc_parse`: pass, 38 tests.
- `cargo test -q --test rc_res`: pass, 31 tests, including Borland oracle runs.
- `cargo test -q --test pe_rsrc`: pass, 9 tests.
- `cargo clippy -- -D warnings`: pass.
- `cargo test -q`: pass.
- Targeted `rustfmt --edition 2024 --check` on touched leaf files: pass.
- `git diff --check`: pass.

Remaining risks: full RailC `.res` byte parity is still blocked by the writer profile gap (source-order emission and default language `0x0000`), then PE/mdlink `.res` integration and named `.rsrc` directory entries. The parser side is now through the whole RailC RC file.

Emotional temperature: noticeably better. Seeing the full RC parse succeed is a real milestone. I am wary of declaring the resource work "basically done" because writer ordering and PE integration can still hide sharp edges, but the front-end uncertainty is much lower now.

## 2026-06-10 18:03

Closed the RC writer profile gap:
- Added `write_res_bc45` as an explicit BC4.5-era serializer while preserving the existing `write_res` brc32 5.40 behavior and tests.
- The BC4.5 profile emits non-STRING resources in source order, emits RT_STRING bundles last, and uses implicit language id `0x0000`.
- Kept the existing payload encoders shared between profiles.
- Fixed BITMAP cooking for BI_RGB DIBs with `biSizeImage == 0`: Borland computes and patches the pixel-data byte count in the RT_BITMAP payload. This was required for `SPLASHWN.BMP`.
- Added a focused BC4.5 profile test for source order / strings-last / lang-zero, and adjusted the inline bitmap tests to cover the zero-`biSizeImage` patch.

Real RailC oracle: generated a scratch BC4.5-profile `.res` from `C:\language\railc\RESOURCE\RAILC.RC` and compared it with `wrk_oracle\railc_golden\railc.res`. Result: `ours_len=121040 gold_len=121040 byte_equal=True`.

Validation:
- `cargo test -q --test rc_parse`: pass, 38 tests.
- `cargo test -q --test rc_res`: pass, 32 tests.
- `cargo test -q --test pe_rsrc`: pass, 9 tests.
- `cargo clippy -- -D warnings`: pass.
- Targeted `rustfmt --edition 2024 --check` on touched leaf files: pass.
- `cargo test -q` twice hit unrelated GUI runtime-test interference in `tests/gui.rs`; `cargo test -q --test gui -- --test-threads=1` passed.
- `cargo test -q -- --test-threads=1`: pass across the full suite.
- `git diff --check`: pass.

Remaining risks: RC file-to-`.res` parity for RailC is now closed. The big remaining resource work is integration: getting `.res` into the PE32 RailC link path, including mdlink `.res` input handling, named `.rsrc` directory entries, all resource types, and the known PE32 resource data-directory offset bug.

Emotional temperature: satisfied but cautious. The byte-for-byte RailC `.res` match is the first hard end-to-end resource artifact in this campaign, which feels substantial. I do not want to blur that into "resources are done" because the linker path can still break the actual executable.

## 2026-06-10 18:42

Continued from the handover into PE/resource integration:
- Reworked PE `.rsrc` construction to consume the same Win32 `.res` record envelope that the RC writer emits, rather than the old ID-only collector helpers.
- Added a small `.res` parser inside `pe_writer`: type/name may be ordinal or UTF-16 string, language and payload are preserved, and metadata not used by PE lookup is ignored.
- Generalized the resource directory emitter for named resource entries. Id-only resources still keep the old byte layout; named entries get `IMAGE_RESOURCE_DIR_STRING_U` records and named-count/id-count headers.
- Wired `link::Input::ResFile` through `link()` and added `pe_writer::merge_res_files_into_pe`, combining one or more `.res` inputs into a single appended `.rsrc` section after object linking.
- Taught `mdlink` to dispatch `.res` files as resource inputs instead of trying to decode them as COFF objects.
- Removed the now-dead `collect_*` PE resource helper structs/functions from `rc::res`.
- Added PE tests for named MENU resources, `.res` input appending, and the PE32 resource data-directory slot so `DataDirectory[2]` is patched without corrupting the Certificate Table directory.

Validation:
- `cargo clippy -- -D warnings`: pass.
- `cargo fmt -- --check`: still fails repo-wide on unrelated pre-existing formatting drift, starting in `src/bin/mdar.rs` and `src/ast.rs`. I did not apply broad formatter churn.
- Targeted `rustfmt --edition 2024 --check ...`: also follows out-of-line `src/link` child modules and reports existing drift in `archive.rs` / `defparse.rs`, so it is not a clean touched-file signal.
- `cargo test -q --test rc_parse`: pass, 38 tests.
- `cargo test -q --test rc_res`: pass, 32 tests.
- `cargo test -q --test pe_rsrc`: pass, 12 tests.
- `cargo test -q -- --test-threads=1`: pass across the full suite.
- `git diff --check`: pass.

Remaining risks: the linker can now append `.res` files structurally, but I have not performed the actual RailC full link in this increment. The next useful step is to feed the real RailC BC4.5-profile `.res` into the 32-bit link path and inspect the resulting executable resource tree, then keep driving the full railc.exe link/run parity.

Emotional temperature: focused and a little wary. The implementation is clean and the old byte oracle stayed green, but PE resources are a place where a correct-looking tree can still fail loader lookup if one count or offset is subtly wrong. The PE32 directory-slot test helps, and I feel better having avoided another formatter-noise mess.

## 2026-06-11 00:27

Continued the W6 RailC startup campaign from the handover:
- Confirmed the prior vtable-slot ABI repair gets `TStartup::SetupWindow` and `TMainWindow::SetupWindow` running instead of dispatching to `TWindow::CleanupWindow`.
- Traced the next startup crash to `TToolbar::TToolbar`: the caller pushed the full `Toolbuttondata` record inline on the i386 cdecl stack, but the callee classified the >8-byte record parameter as a Win64-style hidden pointer and dereferenced the first word as an address.
- Fixed Win32 parameter layout so record parameters keep their declared inline type regardless of Win64 HiddenPtr classification.
- Added `i386_large_struct_by_value_arg_is_inline_not_hidden_pointer` to lock the ABI shape that RailC's toolbar constructor needs.
- After rebuilding release, rerunning the RTL/BIDS sweep, and relinking RailC, the GDB startup trace now gets through the toolbar, all 12 tool buttons, statbar, and several child windows. The process stays responsive until the watchdog timeout instead of crashing.
- The screenshot utility returned a black frame in this session despite the process being responsive; `mdscreensnap --list-monitors` and `--monitor all` also failed internally, so I am treating the screenshot as inconclusive rather than a GUI regression.
- Full tests exposed a vtable-regression from the H5 rebuild changes: derived virtual destructors no longer overrode the base `~` slot because the new signature lookup searched for `Der::~` instead of `Der::~Der`.
- Fixed the vtable rebuild helper to translate the internal destructor key `~` into the real qualified destructor name.
- Added `i386_virtual_destructor_override_survives_vtable_rebuild`; both differential arms for `virtual.c` are green again.
- Re-blessed three O1 PE byte hashes for the intentional `new T` zero-fill change: `cxx_new_delete_scalar`, `cxx_new_class_ctor_and_method`, and `cxx_new_runs_ctor_delete_runs_dtor`. Their behavioral tests were already green; the byte movement is expected because `new` now zeroes storage before construction/use.

Validation so far:
- `cargo test -q --test i386_run i386_large_struct_by_value_arg_is_inline_not_hidden_pointer`: pass.
- `cargo test -q --test i386_run i386_virtual_destructor_override_survives_vtable_rebuild`: pass.
- `cargo build --release`: pass.
- `scripts/resweep_rtl.sh`: pass (`rtl ok=592 skip=35`, `bids ok=22 skip=5`).
- `wrk_probe/closure_railc.ps1`: pass (`railc ok=21`, `owl ok=107`, `streams ok=48`, resource byte parity true, link exit 0).
- `gdb_create_path.txt`: no startup crash before watchdog; toolbar/toolbutton/status/main child creation observed.
- `cargo test -q --test differential o2_msvc_differential`: pass.
- `cargo test -q --test differential o3_bcc32_behavioural`: pass.

Current blockers/risks:
- `cargo clippy -- -D warnings` is blocked by existing `clippy::type_complexity` warnings at `src/codegen.rs:11849` and `src/parser.rs:5432`; I did not churn those unrelated signatures in this RailC startup fix.
- Full `cargo test -q` needs to be rerun after the O1 hash update.
- Visual GUI parity is not proven yet. We have strong startup/lifecycle evidence from GDB and process responsiveness, but the screenshot tool is unreliable in this session.

Emotional temperature: good, with a bit of annoyance at the tooling noise. The toolbar crash was a clean ABI bug with an excellent minimal reproducer, and the destructor fallout was caught by the right oracle before it could hide in RailC. I am wary of calling the GUI good based only on GDB traces; we need a better visual or window-tree oracle next.

## 2026-06-11 00:35

Validation addendum:
- `cargo test -q --test o1_byte_identity`: pass after re-blessing the three intentional `new T` zero-fill PE hashes.
- Full `cargo test -q` then exposed the expected i386 byte-stripe movement for `tests/corpus/portable/virtual.c`.
- Re-blessed only `virtual.c` in `tests/o1_x86_byte_identity.rs` because the virtual-destructor override fix legitimately changes the i386 PE bytes, and both `o2_msvc_differential` and `o3_bcc32_behavioural` are green for the same corpus fixture.

Emotional temperature: still steady. The byte-oracle failures are doing their job; they are noisy, but they forced me to account for every intentional codegen byte movement instead of letting "tests pass" blur the artifact contract.

## 2026-06-11 00:48

Final validation pass for this increment:
- Removed the last clippy blockers with two compile-time-only type aliases (`MfpTarget` in `codegen.rs`, `ClassTemplateArgs` in `parser.rs`).
- `cargo fmt`: pass.
- `cargo clippy -- -D warnings`: pass.
- `cargo test -q`: pass across the full suite.
- `cargo build --release`: pass after the alias cleanup.

RailC runtime/closure status remains as recorded at 00:27/00:35: current release compiler rebuilt the RTL/BIDS archives, relinked RailC with byte-identical resources, and the GDB startup trace gets through main setup, toolbar, all toolbuttons, statbar, and child window creation without crashing.

Remaining risk: GUI visual parity is still not proven because `mdscreensnap` produced a black capture and its monitor helper failed in this shell session. The next useful oracle should be window-tree/paint-state inspection or a repaired screenshot path, not more blind code changes.

Emotional temperature: calmer. The validation gates are clean now, which matters. The only thing still bothering me is the weak visual oracle; the compiler/runtime evidence is much stronger than the screen evidence.

## 2026-06-11 03:32

Continued the GUI parity work and got the first-viewport RailC screen over the line:
- Reproduced the previous weak visual risk as a hard failure: the mdbcc RailC window had the menu and child rectangles, but toolbar icons and the initial rail bitmap were blank while the golden showed both.
- Proved the PE resource tree was not the culprit: key bitmap/menu/icon resources were present in both executables with matching sizes, and runtime `LoadBitmapA` returned non-null handles for `TB_MAINWIN`, `UPBUTTON`, and `DOWNBUTTON`.
- Found a bad early GDB probe assumption: breaking at a function entry before the prologue means `ebp` still belongs to the caller. Correcting that removed the false `HDC=1` lead.
- Dumped the generated vtables and found the real failure: `TToolbutton::Paint` and `TToolbar::Paint` were emitted, but their vtable slots still pointed at `TWindow::Paint`, so `EvPaint` erased the controls and never drew the bitmaps.
- Root cause: virtual signature comparison used raw `Type` equality. A base declaration with forward-declared `TDC`/`TRect` cached `Record { size: 0, align: 1 }`, while the derived declaration saw the completed records. Same record IDs, different cached sizes, so the inherited virtual override was missed.
- Fixed parser virtual signature matching to compare record identity structurally and recurse through refs/pointers/arrays/function/member-function pointer types.
- Also made Win32 parser predefine `bool` as BC++'s 4-byte `int` while leaving the historical default path at 1 byte; this matches the Borland `typedef int bool` world before headers are replayed.
- Added `i386_predefined_borland_bool_matches_bool_alias_virtual_override`, using the OWL/RailC `Paint(TDC&, bool, TRect&)` versus `Paint(TDC&, BOOL, TRect&)` shape with forward-declared records.
- Rebuilt release, reran RailC closure, and verified the regenerated object relocations now put `TToolbutton::Paint` / `TToolbar::Paint` in the vtable slots. GDB dump of the final PE also shows those addresses in the live vtables.
- GUI capture after the fix produced a `CopyFromScreen` PNG that was byte-identical to the golden (`62394` bytes each). After the final RTL/BIDS resweep and relink, `CopyFromScreen` started failing with `The handle is invalid` in this desktop session and produced an empty 1469-byte PNG; a `PrintWindow` capture of the final exe succeeded and visually showed the toolbar icons and rail bitmap, but is not byte-comparable because it frames/renders the window differently.

Validation:
- `cargo test -q i386_predefined_borland_bool_matches_bool_alias_virtual_override`: pass.
- `cargo fmt`: pass.
- `cargo build --release`: pass.
- `wrk_probe/closure_railc.ps1`: pass twice after the fix; final run after rebuilding support libs: `railc ok=21`, `owl ok=107`, `streams ok=48`, resource byte parity true, link exit 0.
- `cargo clippy -- -D warnings`: pass.
- `cargo test -q`: pass across the full suite.
- `scripts/resweep_rtl.sh`: pass (`rtl ok=592 skip=35`, `bids ok=22 skip=5`) and rebuilt `mdcw32.lib`/`mdbids.lib`.

Remaining risk: first-window visual parity is now strong but not perfectly re-captured byte-for-byte on the exact final relink because the screen-copy helper became flaky. The final executable's vtables are fixed and the `PrintWindow` capture is visually correct, so I do not see an active compiler/runtime blocker here; the next tier should exercise interactions/open-file paths rather than keep poking this paint bug.

Emotional temperature: relieved, with some irritation at the screenshot machinery. The bug itself ended up being a clean type-identity issue, and seeing the vtable slot flip from `TWindow::Paint` to the derived methods felt solid. I am still wary of over-claiming "all GUI parity" from one screen, but this is a real milestone: the golden first viewport is no longer aspirational; it rendered.

## 2026-06-11 04:52

Continued from the W6 dialog/shutdown handover:
- Fixed null-preserving base conversions in `convert`: static shifted-base upcasts and shared-vbase upcasts now leave a null source pointer as null instead of adding an offset or loading through a vbptr.
- Fixed scalar and array delete lowering so `delete p` skips destructor/free when the evaluated payload pointer is null. This closes the OWL `delete (TStreamableBase*)DocManager` shutdown crash where `DocManager == 0`.
- Added focused i386 regressions for shifted-base null upcast, virtual-base null upcast, and deleting a null pointer after casting to a secondary base.
- Diagnosed the remaining nonzero RailC process exit as a return-site UDC gap, not a message-loop problem: `WinMain` was preserving `&Manager.Status` instead of invoking `TStatus::operator int()`.
- Fixed scalar return lowering to apply `conv_op_call` for record-to-return-type expressions before spilling the return value across scope-exit destructors.
- Added `i386_return_record_member_to_scalar_uses_conversion_operator`, shaped like RailC's `Manager.Run(); return Manager.Status;` with a destructor that would otherwise clobber the ABI return register.
- Updated intentional byte baselines:
  - `tests/o1_byte_identity.rs`: three `new/delete` PE hashes moved because null-delete now emits a real guard.
  - `tests/o1_x86_byte_identity.rs`: the 20 pinned i386 PE hashes moved together because PE32 optional-header version metadata now matches Borland, which was required for dialog base-unit/font geometry parity.

RailC/dialog results:
- `cargo build --release`: pass.
- `wrk_probe/closure_railc.ps1`: pass (`railc ok=21`, `owl ok=107`, `streams ok=48`, `mdrc exit=0`, resource byte parity true, link exit 0, `railc.exe` 8,737,280 bytes).
- ABOUT capture: size `617x179` vs golden `617x179`, controls equal, `GoldenExit=0`, `MdbccExit=0`, pixel diff still 908.
- CONFIG capture: size `286x257` vs golden `286x257`, controls equal, `GoldenExit=0`, `MdbccExit=0`, pixel diff still 54.

Validation:
- Focused regressions for the null upcasts/delete and return conversion pass.
- `cargo fmt`: pass.
- `cargo clippy`: pass.
- `cargo test -q`: pass across the full suite after the intentional byte-baseline updates.

Remaining risk:
- Dialog functional parity is much stronger now: no shutdown crash, no nonzero exit, matching dialog size and controls. Pixel parity is still not exact (`ABOUT` 908 differing pixels, `CONFIG` 54), so the next useful step is visual-delta inspection rather than another lifecycle hunt.
- The working tree is very dirty from ongoing shared work; I only worked with the existing dirt and did not revert unrelated changes.

Emotional temperature: focused but a little worn down by chained shutdown bugs. The null-cast/delete crash and the `TStatus` return-value bug were both real compiler faults with clean minimal regressions, so the progress feels solid. The remaining pixel deltas are small enough to be annoying rather than scary, but they still need a proper oracle pass before calling dialog parity done.

## 2026-06-11 05:23

Closed the remaining ABOUT/CONFIG dialog pixel parity gap:
- Inspected the CONFIG images and found the residual 54 pixels were not font/geometry; the mdbcc dialog never set the selected radio/checkbox states.
- Probed the OWL dialog setup path with temporary GDB scripts and found the decisive trace:
  - `TDialog::Execute` set `DlgCreationWindow`.
  - The first `WM_SETFONT` message consumed `DlgCreationWindow`, subclassed the dialog, and dispatched through `DialogFunction`.
  - The later `WM_INITDIALOG` did not recover the `TDialog*`, so `TDialog::EvInitDialog` and `TConfigur::SetupWindow` never ran.
- Root cause: the generated `StdDlgProc` call to `::GetWindowPtr(hDlg)` pushed only the explicit `HWND` argument. The C++ prototype has a second defaulted parameter, `const TApplication* app = 0`; because the call was extern-only and emitted through the Borland-mangled symbol, marshalling looked up defaults under the mangled key instead of the source key. The callee read stack garbage as `app`, sent that garbage as the `GetWindowPtrMsgId` lParam, and `TWindow::ReceiveMessage` rejected it unless it happened to be zero or the real application pointer.
- Fixed `gen_call_with_lead` so extern-only calls pre-expand source-name defaults before emitting a call to the mangled extern symbol.
- Added `i386_extern_proto_default_arg_uses_source_defaults_for_mangled_call`, a two-TU regression for `int ext(int, int = 40);` called as `ext(2)` through an extern-only prototype.
- Removed the temporary GDB probe files after the cause was proven.

RailC/dialog results:
- `cargo build --release`: pass.
- `wrk_probe/closure_railc.ps1`: pass (`railc ok=21`, `owl ok=107`, `streams ok=48`, `mdrc exit=0`, resource byte parity true, link exit 0, `railc.exe` 8,737,280 bytes).
- Dialog capture with the fresh exe is exact:
  - ABOUT: `PixelEqual=True`, `DifferingPixels=0`, `ControlsEqual=True`, size `617x179`, exits `0/0`.
  - CONFIG: `PixelEqual=True`, `DifferingPixels=0`, `ControlsEqual=True`, size `286x257`, exits `0/0`.

Validation:
- `cargo test -q i386_extern_proto_default_arg_uses_source_defaults_for_mangled_call`: pass.
- `cargo fmt`: pass.
- `cargo clippy`: pass.
- `cargo test -q`: pass across the full suite.

Remaining risk:
- ABOUT/CONFIG dialog parity is now exact for the current capture tier. This does not prove every RailC GUI workflow; the next useful tier is interaction/open-file/runtime workflow parity rather than more static dialog inspection.
- The worktree remains very dirty from shared W6 work. I avoided reverting or deleting anything unrelated.

Emotional temperature: much better. This was a satisfying bug because the trace was ugly at first but collapsed to one concrete ABI/default-argument mistake. I am annoyed that an apparently tiny pixel diff hid a default-argument miscompile in `StdDlgProc`, but this is exactly why the visual oracle matters.

## 2026-06-11 08:35

Continued the RailC self-host/finish-dialog parity tier:
- Read the W6 handover and resumed from the `istream::getline`/resource-compiler closure state.
- Confirmed the previous `getline(char*)` strong-body fix held: release closure initially passed with resources byte-identical, and the final `istream::getline$qpc` symbol resolved to the real `ISTGLINE.CPP` body.
- Traced the first GUI startup failure with GDB: `filebuf::open` succeeded, `eof()` stayed false, and `getline` read `[SECTIONS]\r`. RailC's `strcmpi(szInput, "[SECTIONS]")` therefore missed the section header.
- Fixed `wrk_rtlshim/rtlio.c` text mode:
  - default opens to text unless `O_BINARY` is present,
  - text `read` strips CR bytes, including a CR at the end of a read buffer,
  - text `write` expands `\n` to CRLF,
  - `close` clears stored mode.
- Added `i386_rtl_io_shim_text_read_strips_crlf`, which includes the actual shim source and verifies text-vs-binary CRLF reads in a WOW64 PE.
- Rebuilt `mdcw32.lib`/`mdbids.lib` through `wrk_probe/resweep_rtl.ps1`. The bash script is not usable under this shell because `bash` is WSL and the script's `C:/...` executable paths do not resolve there; the PowerShell sweep has path guards and worked.
- Regenerating the libraries exposed a stale-archive masking problem: rebuilt BIDS/RTL string objects no longer exported `@string@$brplu$qpc` / `@string@prepend$qpc`, causing RailC link failure.
- Narrowed the inline signed-char/char collision pruning so same-shape inline providers are not suppressed by collapsed `$qpc` prototypes.
- Added a Win32-only reachability root for Borland `cstring.h`'s public one-arg `string` char-pointer inline wrappers. This restores those weak archive exports without polluting Win64 classlib tests.
- Added regressions:
  - matching external `char*` inline body survives,
  - Borland string char-pointer inline wrappers export weak `$qpc` symbols,
  - original signed-char wrapper collision still prunes correctly.
- After the CRLF fix, the GUI harness reached the finish dialog but showed `15635664 trains` where golden showed `0 trains`.
- Root cause was i386 cdecl default argument promotion: `sprintf("%.2f ... %d", float, int)` passed the float through `...` as a 4-byte float. The CRT consumed the following int as the high half of the `%f` double, then read garbage for `%d`.
- Fixed `marshal_args_cdecl` so an FP argument with no declared parameter type promotes a 4-byte `float` to an 8-byte `double` stack slot.
- Added `i386_variadic_float_promotes_to_double_slot`, using Borland-style raw stdarg stack walking to avoid the separate i386 `va_arg(double)` intrinsic gap.

Results:
- `wrk_probe/closure_railc.ps1`: pass on current release (`railc ok=21`, `owl ok=107`, `streams ok=48`, resources byte-identical, link exit 0, `railc.exe` 9,030,144 bytes).
- `wrk_probe/gui_parity/capture_finish_parity.ps1`: exact finish-dialog parity (`PixelEqual=True`, `DifferingPixels=0`, `ControlsEqual=True`, size `381x174`).
- `wrk_probe/resweep_rtl.ps1`: pass (`rtl ok=592 skip=35`, `bids ok=22 skip=5`, `mdcw32.lib` 594 members / 1351 symbols, `mdbids.lib` 22 members / 1270 symbols).

Validation:
- Focused tests for RTL text read, inline wrapper/collision handling, and variadic float promotion: pass.
- `cargo fmt`: pass.
- `cargo clippy`: pass.
- `cargo test -q`: pass across the full suite.
- `cargo build --release`: pass after the final Win64 gate fix.

Remaining risk:
- The finish-dialog capture tier is now exact, and this gets past the previous `[SECTIONS]` error. This still does not prove longer gameplay parity; next useful tiers should drive more runtime interactions after start.
- The i386 `va_arg(double)` intrinsic itself still has a separate Win32 encoder gap; I avoided that path in the new regression because RailC's observed bug was caller-side float promotion.
- The repo remains heavily dirty from shared W6 work. I worked with existing changes and did not revert unrelated files.

Emotional temperature: pleased but wary. The CRLF bug was predicted in the shim comments and felt good to close. The archive-string detour was frustrating because old objects hid the issue until the honest rebuild, but the resulting guard is contained. The finish-dialog `%f`/`%d` bug was the cleanest moment: the visual oracle pointed straight at a real ABI rule, and the zero-pixel final capture is a solid milestone.

## 2026-06-11 09:10

Continued the RailC child-window parity tier:
- Reproduced the child-window parity state after the virtual cdecl record-to-scalar conversion fix. The windows all paint and have matching controls/titles/sizes, but text pixels still differed.
- Proved the old child-window harness was nondeterministic: golden-vs-golden differed because RailC seeds its RNG with `randomize()`/`time(NULL)` once the layout starts.
- Found a real mdbcc miscompile in Borland's inline `randomize()`: under the full `classdef.h` include graph, `expr_type(time(0))` resolved the call as construction of `struct time` rather than the visible `time()` function. That made `(unsigned)time(0)` look like a record-to-scalar cast and the i386 cdecl marshaller pushed the address of a materialized temporary to `srand`.
- Fixed `expr_type`'s `Expr::Call` arm to use the same function-hides-tag gate as codegen: visible `funcs`, `overloads`, or `extern_symbols` suppress the `find_record_id_by_tag` constructor path.
- Added/updated `i386_cdecl_scalar_arg_cast_call_result_passes_value` so it includes both `struct time` and a C-linkage `time()` function, matching the RailC header collision.
- Verified the original `classdef.h` probe now emits `randomize()` as value flow: call `time`, store/reload EAX, push EAX to `srand`; no `lea [ebp-...]` temporary address.
- Rebuilt release compiler, RTL/BIDS, and RailC closure:
  - `cargo build --release`: pass.
  - `wrk_probe/resweep_rtl.ps1`: pass (`rtl ok=592 skip=35`, `bids ok=22 skip=5`).
  - `wrk_probe/closure_railc.ps1`: pass (`railc ok=21`, `owl ok=107`, `streams ok=48`, resources byte-identical, link exit 0, `railc.exe` 9,030,144 bytes).
- Confirmed the linked `wrk_probe\rb\railc.exe` has the corrected `randomize()` body at `0x004714c8`.
- Reworked `wrk_probe\gui_parity\capture_child_windows_parity.ps1` to pair-drive golden and mdbcc: both processes are brought to the start dialog, OK is posted just after a second boundary, and timer ticks are sent to both layouts in lockstep.
- Golden-vs-golden with the paired harness is now exact for arrivals/departures/platforms; locoyard still has a small capture artifact.
- Found that the default harness golden was stale relative to `C:\language\railc\build\railc.exe`. Against the staged current Borland exe, departures are exact, arrivals/platforms differ only in loco IDs, and locoyard remains in small artifact territory.
- Built a scratch RNG/time probe with both BCC32 and mdbcc, then removed it. After fixing the probe's own argument-evaluation trap, `time`, `_lrand`, `rand`, and `random(num)` matched exactly for the same seed.

Validation:
- Focused regression `i386_cdecl_scalar_arg_cast_call_result_passes_value`: pass.
- `cargo fmt`: pass.
- `cargo clippy`: pass.
- `cargo test -q`: pass across the full suite.

Remaining risk:
- Current child-window parity is much closer, but not done. The remaining arrival/platform differences are locomotive IDs with otherwise matching rows/statuses. Since seed and RNG primitives match, the next likely issue is different RNG consumption or eligibility decisions before/inside loco assignment.
- The paired harness fix lives under ignored `wrk_probe`; keep it in mind when comparing results. The default `golden_run` also appears stale; use or refresh from the current Borland build before drawing conclusions.
- The repo is still heavily dirty from shared W6 work. I removed only scratch files I created and did not revert unrelated changes.

Emotional temperature: mildly annoyed but clearer. The false trail through RNG seeding ate time, but it paid off by exposing a concrete function/tag collision in the compiler and a stale-baseline problem in the harness. The remaining loco-ID mismatch feels tractable, not mysterious, but it needs a better oracle than visual pixels alone.

## 2026-06-11 09:32

Continued the RailC child-window parity tier after the `randomize()` compiler fix:
- Refreshed the standard 120-tick child-window capture against the current Borland RailC baseline. Departures were exact, but arrivals/platforms/locoyard still showed locomotive-number pixel diffs.
- Inspected the rendered windows directly. The schedules, statuses, descriptions, titles, controls, and sizes matched; only assigned/free locomotive numbers differed.
- Added `wrk_probe/gui_parity/trace_loco_assignment_pair.ps1`, a paired GDB trace that uses mdbcc map symbols and Borland TDUMP public symbols. It logs `TLayout::AssignLoco`, `TTimetable::SetLoco`, `TPlatData::SetLoco`, and decodes `TLocos::SetFlag` objects into real class/number/type/flag values.
- Fixed the trace harness while validating it:
  - Borland public offsets are based at the `CODE` section VMA `0x00410000`, not the mdbcc-style `0x00401000`.
  - The prepared `child_window_capture/golden_run` directory must be used, not the raw `golden_current_run`, because the prepared directory contains the matching `RC.INI` data-file path.
- Proved initial load/start assignment parity. With prepared run dirs, golden and mdbcc selected the same first eight timetable locos and the same first three yard locos.
- Proved the remaining visual mismatch was harness nondeterminism, not a compiler/runtime divergence. `randomize()` happens during process/window initialization, before the "Prepare to start" OK synchronization. The old pair harness launched golden, waited all the way to the start dialog, then launched mdbcc; the first mdbcc launch after refreshing the large exe often crossed a one-second boundary and seeded differently.
- Reworked `wrk_probe/gui_parity/capture_child_windows_parity.ps1`:
  - split process launch from ready-dialog completion,
  - launch golden and mdbcc back-to-back before waiting on either process,
  - add a warm launch after refreshing run dirs so first-launch/Defender/cache delay is paid outside the measured captures.
- Reran the standard 120-tick child-window capture. Result: arrivals, departures, platforms, and locoyard are all pixel-identical (`DifferingPixels=0`, controls/titles/sizes equal, both exit 0).

Validation:
- `wrk_probe/gui_parity/capture_child_windows_parity.ps1 -GoldenRun wrk_probe/gui_parity/golden_current_run`: pass for all four child windows with zero differing pixels.
- The paired GDB trace confirms same assignment/free-list behavior under matched prepared data and same launch timing.
- No Rust source was changed in this slice, so I did not rerun the full Rust gate here. The previous compiler change already had `cargo fmt`, `cargo clippy`, and `cargo test -q` green.

Remaining risk:
- The child-window visual tier is now exact for this harness and `KINGSX.RCD` after 120 synthetic timer ticks. This still does not prove deeper interactive/gameplay workflows, save/load paths, or longer-running timer behavior.
- The important harness fixes live under ignored `wrk_probe`; future comparisons must keep using this warmed, pair-launched capture path or they can recreate false RNG mismatches.
- The repo remains broadly dirty from shared W6 work. I did not revert unrelated changes.

Emotional temperature: relieved and a little irritated at the harness. The compiler looked guilty because the pixels were real, but the paired assignment trace did its job: it separated deterministic program behavior from a bad oracle. The zero-pixel child-window run is a solid milestone.

## 2026-06-11 09:45

Pushed the RailC GUI parity evidence beyond the early child-window snapshot:
- Re-read the active goal doc to keep the acceptance scope honest. The current frontier is W6 run-and-match, not merely compile/link.
- Ran the warmed child-window parity scenario at `TimerTicks=720` against the current Borland baseline. All four child windows stayed exact:
  - Arrivals: `PixelEqual=True`, `DifferingPixels=0`
  - Departures: `PixelEqual=True`, `DifferingPixels=0`
  - Platforms: `PixelEqual=True`, `DifferingPixels=0`
  - Loco yard: `PixelEqual=True`, `DifferingPixels=0`
- Added an opt-in profile-save parity mode to `wrk_probe/gui_parity/capture_child_windows_parity.ps1`:
  - `-CompareSavedIni` enables `Save on exit`,
  - both builds read the same workspace-local `.RCD` path so the saved profile should be byte-comparable,
  - after the normal visual pass the harness compares `golden_run/RC.INI` and `mdbcc_run/RC.INI` byte-for-byte and writes `profile_results.json`.
- Rebuilt the all-mdbcc RailC closure with `wrk_probe/closure_railc.ps1` after the harness change:
  - `railc ok=21`, `owl ok=107`, `streams ok=48`
  - `mdrc exit=0`
  - `.res` byte parity true (`ours_len=121040`, `gold_len=121040`)
  - link exit 0
  - `railc.exe` size 9,030,144 bytes
- Reran the 720-tick visual + saved-profile parity after closure refreshed `wrk_probe/rb/railc.exe`. It passed again, and the saved `RC.INI` files are byte-identical (`ProfileIniEqual=True`, length 473, first difference -1).

Validation:
- `mdtimeout 1200s -- powershell -NoProfile -ExecutionPolicy Bypass -File .\wrk_probe\closure_railc.ps1`: pass.
- `mdtimeout 420s -- powershell -NoProfile -ExecutionPolicy Bypass -File .\wrk_probe\gui_parity\capture_child_windows_parity.ps1 -GoldenRun .\wrk_probe\gui_parity\golden_current_run -TimerTicks 720 -CompareSavedIni`: pass.

Remaining risk:
- This is stronger than the previous 120-tick Tier 2 result and now covers profile saving on clean exit, but it is not full Tier 3 save-game parity because RailC does not appear to persist simulation state beyond `RC.INI`.
- Still not a complete W6 proof: longer ad-hoc interactions such as pause/stop/restart, set-data-file dialog behavior, and alternate `.RCD` scenarios remain unverified.
- No Rust source changed in this slice, so I did not rerun `cargo fmt`, `cargo clippy`, or `cargo test -q`.

Emotional temperature: steady. The 720-tick pass after a full closure rebuild makes the GUI evidence feel less fragile. The profile-save check is small but valuable because it exercises the app's clean-exit path and Win32 profile writes without broadening the acceptance target into speculation.

## 2026-06-11 09:50

Expanded the child-window parity coverage across alternate RailC `.RCD` data sets:
- Staged the available `C:\language\railc\*.RCD` files into the workspace-local `wrk_probe/gui_parity/golden_current_run` directory so the current Borland executable and the current mdbcc executable can be driven from identical data inputs without writing outside the repo.
- Ran the warmed 720-tick visual + saved-profile parity scenario on additional data files:
  - `QUEENST.RCD`: all four child windows exact; saved `RC.INI` byte-identical (474 bytes).
  - `YORK.RCD`: all four child windows exact; saved `RC.INI` byte-identical (471 bytes).
- Tried `WAVERLY88.RCD`, the largest staged data file. The first three windows matched exactly, then the harness failed because the golden build itself did not open Loco yard at that point. This was a parity-harness assumption, not evidence of a compiler mismatch.
- Added strict opt-in missing-window comparison to `capture_child_windows_parity.ps1` via `-AllowMissingWindows`. Default behavior remains strict. With the flag, a window is considered parity only if both builds miss it; a one-sided miss still fails.
- Reran `WAVERLY88.RCD` with `-AllowMissingWindows`. Result:
  - Arrivals/departures/platforms: `PixelEqual=True`, `DifferingPixels=0`
  - Loco yard: `PixelEqual=True`, reason `missing in both builds`
  - saved `RC.INI` byte-identical (476 bytes)

Validation:
- `mdtimeout 420s -- powershell -NoProfile -ExecutionPolicy Bypass -File .\wrk_probe\gui_parity\capture_child_windows_parity.ps1 -GoldenRun .\wrk_probe\gui_parity\golden_current_run -MdbccRun .\wrk_probe\gui_parity\golden_current_run -DataFile QUEENST.RCD -TimerTicks 720 -CompareSavedIni`: pass.
- `mdtimeout 480s -- powershell -NoProfile -ExecutionPolicy Bypass -File .\wrk_probe\gui_parity\capture_child_windows_parity.ps1 -GoldenRun .\wrk_probe\gui_parity\golden_current_run -MdbccRun .\wrk_probe\gui_parity\golden_current_run -DataFile YORK.RCD -TimerTicks 720 -CompareSavedIni`: pass.
- `mdtimeout 480s -- powershell -NoProfile -ExecutionPolicy Bypass -File .\wrk_probe\gui_parity\capture_child_windows_parity.ps1 -GoldenRun .\wrk_probe\gui_parity\golden_current_run -MdbccRun .\wrk_probe\gui_parity\golden_current_run -DataFile WAVERLY88.RCD -TimerTicks 720 -CompareSavedIni -AllowMissingWindows`: pass.

Remaining risk:
- The broadened Tier 2 coverage is now much stronger, but it is still sampled coverage: not every `.RCD`, every command, every modal path, or every minute of every shift.
- `-AllowMissingWindows` should be used only for scenarios where the golden refuses a child window too; the strict default remains safer for the canonical KINGSX tier.
- No Rust source changed in this slice, so no Rust gate was rerun.

Emotional temperature: good. The failed Waverly run was a useful reminder that a parity oracle needs to compare absence as well as pixels. Turning that into an explicit opt-in result makes the harness more honest instead of more permissive.

## 2026-06-11 09:56

Added and ran a command-flow parity tier for pause/resume/stop/restart:
- Read the RailC command handlers in `RAILC.CPP`:
  - `CMMnuFilNew` starts/restarts the game, reloads data, enables pause/stop, shows the start dialog, and sets the timer.
  - `CMMnuFiPaus` toggles the game timer and toolbar/menu state.
  - `CMMnuFiStop` stops the timer, clears layout data, disables pause/stop, and invalidates child windows.
- Extended `wrk_probe/gui_parity/capture_child_windows_parity.ps1` with an opt-in `-ExerciseControlFlow` path:
  - drive a small number of timer ticks,
  - send Pause,
  - send Restart/Resume,
  - drive a small number of additional ticks,
  - send Stop,
  - send Start New and dismiss the modal start dialog,
  - then perform the usual timer advance, child-window capture, and optional saved-INI byte comparison.
- Kept this off by default so the canonical visual tier remains the simple Start New + advance-clock scenario.

Validation:
- `mdtimeout 600s -- powershell -NoProfile -ExecutionPolicy Bypass -File .\wrk_probe\gui_parity\capture_child_windows_parity.ps1 -GoldenRun .\wrk_probe\gui_parity\golden_current_run -DataFile KINGSX.RCD -TimerTicks 240 -CompareSavedIni -ExerciseControlFlow -ExercisePreTicks 60 -ExercisePostTicks 30`: pass.
  - arrivals/departures/platforms/locoyard all exact (`DifferingPixels=0`)
  - saved `RC.INI` byte-identical (473 bytes)
- `mdtimeout 660s -- powershell -NoProfile -ExecutionPolicy Bypass -File .\wrk_probe\gui_parity\capture_child_windows_parity.ps1 -GoldenRun .\wrk_probe\gui_parity\golden_current_run -MdbccRun .\wrk_probe\gui_parity\golden_current_run -DataFile YORK.RCD -TimerTicks 240 -CompareSavedIni -ExerciseControlFlow -ExercisePreTicks 60 -ExercisePostTicks 30`: pass.
  - arrivals/departures/platforms/locoyard all exact (`DifferingPixels=0`)
  - saved `RC.INI` byte-identical (471 bytes)

Remaining risk:
- This materially improves command-routing/state-cleanup coverage, but it is still a sampled interaction tier. It does not prove every toolbar path, file dialog path, help path, or a full end-of-shift playthrough.
- The command-flow harness sends synthetic timer messages, as the rest of the GUI parity harness does. It validates deterministic app state transitions under the existing oracle, not real-time wall-clock pacing.
- No Rust source changed in this slice, so no Rust gate was rerun.

Emotional temperature: confident. This is the kind of incremental oracle broadening that matters: it exercises real user commands without changing the acceptance corpus or pretending a sampled pass is full completion.

## 2026-06-11 10:04

Added and validated a Set data file cancel parity probe:
- Extended `wrk_probe/gui_parity/capture_child_windows_parity.ps1` with opt-in `-ExerciseSetDataCancel`.
- The probe opens the `CM_MNUFILSETD` common file dialog after the main window is ready, records the dialog title and child controls, sends Cancel, waits for the dialog to close, then continues into the existing Start New/timer/child-window/profile parity flow.
- Trace evidence confirms both golden and mdbcc builds opened the same `Rail Control data file` dialog shape and both canceled cleanly.

Validation:
- `mdtimeout 600s -- powershell -NoProfile -ExecutionPolicy Bypass -File .\wrk_probe\gui_parity\capture_child_windows_parity.ps1 -GoldenRun .\wrk_probe\gui_parity\golden_current_run -DataFile KINGSX.RCD -TimerTicks 120 -CompareSavedIni -ExerciseSetDataCancel`: pass.
  - arrivals/departures/platforms/locoyard all exact (`DifferingPixels=0`)
  - saved `RC.INI` byte-identical (473 bytes)
- `mdtimeout 600s -- powershell -NoProfile -ExecutionPolicy Bypass -File .\wrk_probe\gui_parity\capture_child_windows_parity.ps1 -GoldenRun .\wrk_probe\gui_parity\golden_current_run -MdbccRun .\wrk_probe\gui_parity\golden_current_run -DataFile YORK.RCD -TimerTicks 120 -CompareSavedIni -ExerciseSetDataCancel -ExerciseControlFlow -ExercisePreTicks 60 -ExercisePostTicks 30`: pass.
  - dialog cancel plus pause/resume/stop/restart path passed
  - arrivals/departures/platforms/locoyard all exact (`DifferingPixels=0`)
  - saved `RC.INI` byte-identical (471 bytes)

Remaining risk:
- This covers canceling the file-open common dialog, not selecting a different data file. The open/select path would need either more reliable common-dialog automation or a lower-level hook around `GetOpenFileName`.
- The harness exercises the dialog once per child-window capture because each capture gets a fresh RailC process; that is intentionally repetitive evidence, but it does make the run slower.
- No Rust source changed in this slice, so no Rust gate was rerun.

Emotional temperature: calm and slightly impatient. The cancel path passed cleanly, which is useful, but it also underlines that remaining progress is now mostly about choosing the next thin slice of user behavior rather than fixing obvious compiler defects.

## 2026-06-11 10:14

Investigated the Set data file select path after the cancel path passed:
- Added experimental `-ExerciseSetDataSelectPath` support to `wrk_probe/gui_parity/capture_child_windows_parity.ps1`.
- The harness now records detailed common-dialog child controls, sets the file-name control (`id=1148`) through common-dialog/control APIs, attempts a user-like key accept if available, falls back to button click, and fails the run if saved `RC.INI` does not contain the selected path.
- This assertion is important: without it, both builds can remain on the original `.RCD` and still look like visual/profile parity.

Validation:
- Re-ran the validated cancel path after the extra instrumentation:
  - `mdtimeout 600s -- powershell -NoProfile -ExecutionPolicy Bypass -File .\wrk_probe\gui_parity\capture_child_windows_parity.ps1 -GoldenRun .\wrk_probe\gui_parity\golden_current_run -DataFile KINGSX.RCD -TimerTicks 120 -CompareSavedIni -ExerciseSetDataCancel`: pass.
  - arrivals/departures/platforms/locoyard all exact (`DifferingPixels=0`)
  - saved `RC.INI` byte-identical (473 bytes)
- Tried selecting `YORK.RCD` from `KINGSX.RCD`:
  - `mdtimeout 600s -- powershell -NoProfile -ExecutionPolicy Bypass -File .\wrk_probe\gui_parity\capture_child_windows_parity.ps1 -GoldenRun .\wrk_probe\gui_parity\golden_current_run -MdbccRun .\wrk_probe\gui_parity\golden_current_run -DataFile KINGSX.RCD -TimerTicks 120 -CompareSavedIni -ExerciseSetDataSelectPath .\wrk_probe\gui_parity\golden_current_run\YORK.RCD`: expected fail.
  - common-dialog filename edit contained the intended selected path, but both saved profiles still contained the original `KINGSX.RCD`.
  - `GoldenSelectedDataFileSaved=False`, `MdbccSelectedDataFileSaved=False`.
- Tried selecting a copy of `YORK.RCD` from the dialog's current capture directory:
  - `mdtimeout 600s -- powershell -NoProfile -ExecutionPolicy Bypass -File .\wrk_probe\gui_parity\capture_child_windows_parity.ps1 -GoldenRun .\wrk_probe\gui_parity\golden_current_run -MdbccRun .\wrk_probe\gui_parity\golden_current_run -DataFile KINGSX.RCD -TimerTicks 120 -CompareSavedIni -ExerciseSetDataSelectPath .\wrk_probe\gui_parity\child_window_capture\YORK.RCD`: expected fail.
  - same result: selected path visible in the edit control, original `KINGSX.RCD` saved by both builds.

Interpretation:
- This is harness/OS automation limitation evidence, not a compiler mismatch. Golden and mdbcc behave the same, and both runs fail only the new "selected path actually stuck" oracle.
- `System.Windows.Forms.SendKeys.SendWait` is blocked in this session with `Access is denied`, so a true user-like accept cannot be used from this harness.
- Posting `IDOK`/button click after programmatically setting the edit closes the common dialog but appears to return as Cancel to RailC.

Remaining risk:
- The Set data file open/select success path remains unverified. The cancel path is covered; loading alternate `.RCD` files through seeded `RC.INI` is covered; selecting an alternate `.RCD` through the common dialog is not.
- A future path would need UI Automation that can invoke the Open button as a real UI action, a small debugger/hook around `GetOpenFileName`, or manual confirmation. I would not spend compiler-fix time here until there is evidence of a golden-vs-mdbcc split.
- No Rust source changed in this slice, so no Rust gate was rerun.

Emotional temperature: mildly frustrated but clear. The failed selection probe was useful because it prevented a false green result. The honest outcome is narrower: cancel is proven, select is not.

## 2026-06-11 10:29

Re-established current closure and hardened Tier 1 GUI parity harnesses after the dialog/select work:
- Re-read the goal doc and checked the RailC makefile authority. The goal doc still says 22 RailC TUs, but `C:\language\railc\railc.mak` currently lists 21 object/source entries. Current closure's `railc ok=21` matches the makefile, not a missing compile.
- Ran the current closure with the release compiler:
  - `railc ok=21`
  - `owl ok=107`
  - `streams ok=48`
  - `mdrc exit=0`
  - resource byte parity true (`ours_len=121040`, `gold_len=121040`)
  - link exit 0
  - `railc.exe` size 9,030,144 bytes
- Updated `wrk_probe/gui_parity/capture_dialog_parity.ps1` to use isolated run directories and patch both executables to read local `RC.INI`, matching the newer child-window/finish harness style. This fixed a false CONFIG mismatch caused by stale/reused run-dir state.
- Updated `wrk_probe/gui_parity/capture_finish_parity.ps1` to close the main window normally after dismissing the finish dialog and to fail if either build needs a kill fallback.
- Added `wrk_probe/gui_parity/capture_main_window_parity.ps1`, an isolated main-window pixel oracle for the launch surface.

Validation:
- `mdtimeout 1200s -- powershell -NoProfile -ExecutionPolicy Bypass -File .\wrk_probe\closure_railc.ps1`: pass.
- `mdtimeout 420s -- powershell -NoProfile -ExecutionPolicy Bypass -File .\wrk_probe\gui_parity\capture_dialog_parity.ps1`: pass.
  - ABOUT: `PixelEqual=True`, `DifferingPixels=0`, controls equal, exits `0/0`.
  - CONFIG: `PixelEqual=True`, `DifferingPixels=0`, controls equal, exits `0/0`.
- `mdtimeout 420s -- powershell -NoProfile -ExecutionPolicy Bypass -File .\wrk_probe\gui_parity\capture_finish_parity.ps1`: pass.
  - FINISH: `PixelEqual=True`, `DifferingPixels=0`, controls equal, exits `0/0`, both closed by `wm_close`.
- `mdtimeout 420s -- powershell -NoProfile -ExecutionPolicy Bypass -File .\wrk_probe\gui_parity\capture_main_window_parity.ps1`: pass.
  - main window: `PixelEqual=True`, `DifferingPixels=0`, size `720x480`, title `Rail Control`, exits `0/0`.
- `mdtimeout 600s -- powershell -NoProfile -ExecutionPolicy Bypass -File .\wrk_probe\gui_parity\capture_child_windows_parity.ps1 -GoldenRun .\wrk_probe\gui_parity\golden_current_run -DataFile KINGSX.RCD -TimerTicks 720 -CompareSavedIni`: pass.
  - arrivals/departures/platforms/locoyard all exact (`DifferingPixels=0`)
  - saved `RC.INI` byte-identical (473 bytes)

Remaining risk:
- These are stronger current Tier 1/Tier 2 oracles, but still sampled GUI coverage. They do not prove every menu item, toolbar button, help path, file-open select success, or a full real-time shift.
- The closure still depends on `wrk_oracle\mdcw32.lib` and `wrk_oracle\mdbids.lib`, which are mdbcc-built caches from `scripts/resweep_rtl.sh`. For a final completion audit, rerun the full RTL/BIDS resweep with the current compiler immediately before the final closure/parity gate.
- No Rust source changed in this slice, so no Rust gate was rerun.

Emotional temperature: steadier. The false CONFIG red was a useful warning: old harness state can look like a compiler regression. Isolating the dialog harness and getting clean main/about/config/finish/current-child evidence makes the parity story much less hand-wavy.

## 2026-06-11 10:50

Resumed after compaction and recorded the final-audit style closure/parity batch:
- `mdtimeout 900s -- cargo build --release`: pass.
- `mdtimeout 1800s -- bash scripts/resweep_rtl.sh`: failed immediately in this PowerShell/Git-Bash environment because `bash` could not see `C:/language/mdbcc/target/release/bcc.exe`. I treated this as a shell/path harness issue, not a compiler signal.
- `mdtimeout 1800s -- powershell -NoProfile -ExecutionPolicy Bypass -File .\wrk_probe\resweep_rtl.ps1`: pass.
  - RTL `ok=592 skip=35`.
  - BIDS `ok=22 skip=5`.
  - regenerated `wrk_oracle\mdcw32.lib` with 594 members, 1351 symbols, 2,490,048 bytes.
  - regenerated `wrk_oracle\mdbids.lib` with 22 members, 1270 symbols, 871,730 bytes.
- `mdtimeout 1200s -- powershell -NoProfile -ExecutionPolicy Bypass -File .\wrk_probe\closure_railc.ps1`: pass.
  - `railc ok=21`, `owl ok=107`, `streams ok=48`.
  - `mdrc exit=0`.
  - resource byte parity true (`ours_len=121040`, `gold_len=121040`).
  - link exit 0.
  - regenerated `wrk_probe\rb\railc.exe` at 9,030,144 bytes.

GUI parity after the fresh closure:
- First main-window capture failed with `PixelEqual=False`, `DifferingPixels=249146`; image inspection showed the mdbcc client area missing the rail bitmap while menus and toolbar icons were present. Immediate rerun passed, so I treated it as a cold first-paint race in the harness.
- Updated `wrk_probe\gui_parity\capture_main_window_parity.ps1` to warm-launch both golden and mdbcc before the measured capture.
- Re-ran main-window parity: pass (`PixelEqual=True`, `DifferingPixels=0`, `720x480`, title `Rail Control`, exits `0/0`).
- Re-ran focused GUI gates:
  - ABOUT exact, CONFIG exact, controls equal, exits `0/0`.
  - FINISH exact, controls equal, exits `0/0`, both through normal `wm_close`.
  - KINGSX 720 ticks exact for arrivals/departures/platforms/locoyard, controls/titles equal, exits `0/0`, saved `RC.INI` byte-identical at 473 bytes.

Rust gate status:
- `cargo clippy --all-targets -- -D warnings` initially failed only on `tests\rc_res.rs` because `push_version_fixed` had eight parameters and tripped `clippy::too_many_arguments`.
- Refactored that test helper through a private `VersionFixed` struct. This is test-only cleanup; no compiler behavior changed.
- `mdtimeout 300s -- cargo fmt`: pass.
- `mdtimeout 1800s -- cargo clippy --all-targets -- -D warnings`: pass.
- First `mdtimeout 2400s -- cargo test -q` failed in two GUI tests, matching the already-documented parallel-load GUI flake pattern.
- `mdtimeout 900s -- cargo test -q --test gui -- --test-threads=1`: pass.
- `mdtimeout 3000s -- cargo test -q -- --test-threads=1`: pass.
- Final retry of exact `mdtimeout 2400s -- cargo test -q`: pass.

Remaining risk:
- The current self-host evidence is strong: current compiler, current mdbcc-built RTL/BIDS caches, byte-identical RailC resources, clean link, and focused GUI parity are all green.
- The handover's heavier release gate (`cargo test --release`) and explicit o1_x86 stripe should still be run before calling the goal done.
- GUI parity is still sampled. File-open select success through the common dialog remains unverified because the harness can prove cancel and seeded alternate data files, but not a true user-style Open selection in this session.

Emotional temperature: focused and cautious. The evidence is now good enough that the remaining work feels like final audit discipline rather than chasing an obvious compiler fault, but I do not want to overclaim the goal until the release/stripe gates and the last parity caveats are explicit.

## 2026-06-11 10:59

Ran the final release/oracle gates after the closure/parity batch:
- `mdtimeout 1800s -- cargo clippy --release --all-targets -- -D warnings`: pass.
- `mdtimeout 900s -- cargo test --release --test o1_x86_byte_identity`: pass (`siphash_known_answer`, `x86_byte_identity_corpus`).
- First `mdtimeout 3600s -- cargo test --release`: failed in the same Windows GUI parallel-load pattern seen earlier:
  - `o6_e5d_real_stattest_creates_children_and_clean_exits` found 0 static child controls.
  - `o6_e6_ddvt_lbuttondown_dispatches_and_clean_exits` did not exit within 5 seconds after the injected `WM_LBUTTONDOWN`.
- `mdtimeout 900s -- cargo test --release --test gui -- --test-threads=1`: pass, 40/40.
- `mdtimeout 3600s -- cargo test --release -- --test-threads=1`: pass.
- Retried exact `mdtimeout 3600s -- cargo test --release`: pass.

Provenance/import audit:
- `wrk_probe\closure_railc.ps1` resets `wrk_probe\rb`, compiles RailC from `C:\language\railc` sources with `target\release\bcc.exe`, compiles OWL/streams from `C:\tmp\bc45\SOURCE`, archives those with `target\release\mdar.exe`, builds `railc.res` with `target\release\mdrc.exe`, then links with `target\release\mdlink.exe`.
- Link inputs are the mdbcc-built RailC objects, `mdowl.lib`, `mdstreams.lib`, the freshly reswept `wrk_oracle\mdcw32.lib` and `wrk_oracle\mdbids.lib`, and the mdrc-produced `.res`.
- `objdump -p .\wrk_probe\rb\railc.exe` reports PE32 GUI, `.rsrc` present, and imports only Windows system DLLs plus `CTL3D32.dll`: `KERNEL32.dll`, `GDI32.dll`, `USER32.dll`, `COMDLG32.dll`, `CTL3D32.dll`, `WINMM.dll`, and `SHELL32.dll`.

Interpretation:
- The literal release gate is green after a retry, and the serial release pass confirms the transient red was GUI test scheduling/isolation rather than a compiler/runtime split.
- The all-mdbcc provenance story is now coherent: no Borland runtime DLL import, no Borland object/library input in the final link path, and the resource compiler output remains byte-identical to the golden `.res`.
- Remaining caveat is coverage, not a known mismatch: GUI parity is sampled, and common-dialog file-open select success remains unverified by automation in this session.

Emotional temperature: relieved but still careful. This is the first point where I would be comfortable telling Arthur the self-host target is achieved with explicit caveats, rather than merely "very close".

## 2026-06-11 11:02

Added one longer Tier-3-style GUI audit before closing the goal:
- `mdtimeout 1200s -- powershell -NoProfile -ExecutionPolicy Bypass -File .\wrk_probe\gui_parity\capture_child_windows_parity.ps1 -GoldenRun .\wrk_probe\gui_parity\golden_current_run -DataFile KINGSX.RCD -TimerTicks 3600 -CompareSavedIni`: pass.
- Arrivals, departures, platforms, and loco yard were all exact (`PixelEqual=True`, `DifferingPixels=0`), with matching controls/titles and clean exits.
- Saved `RC.INI` was byte-identical (`ProfileIniEqual=True`, 473 bytes, first difference -1).

Final read on the goal:
- The goal document's "22 TUs" count is stale against the stated authority; `C:\language\railc\railc.mak` and the current closure both use 21 RailC `.CPP` TUs.
- Tier 1 GUI parity is green: main window plus ABOUT/CONFIG/FINISH dialogs are exact.
- Tier 2 GUI parity is green: KINGSX child-window scenario advances the clock and diffs all four child windows exactly against golden.
- Tier 3 stretch evidence is green for saved-profile byte parity and a longer 3600-tick KINGSX run.
- The only explicit caveat I would still carry is common-dialog file-open select success. The cancel path and seeded alternate data-file loads are covered; true selection through the Windows common dialog was blocked by harness/UI automation limitations, and both golden and mdbcc behaved the same under the failed automation.

Git/status note:
- The worktree remains broadly dirty from the in-progress campaign. I did not run `git add`, `git commit`, or `git push` because Arthur's current AGENTS instructions explicitly forbid those commands.

Emotional temperature: satisfied, with a small residue of skepticism about GUI exhaustiveness. The acceptance tiers are green, the provenance audit is clean, and the remaining caveat is not evidence of a mismatch.
