# Resolve Quality Next Steps Journal

## 2026-06-19

- Started in dedicated worktree
  `C:\worktrees\mdbcc\resolve-quality-next-steps` on
  `task/resolve-quality-next-steps`.
- Read the required workflow file and confirmed the hard design/sign-off gate:
  no shipped-code edits before explicit approval.
- Re-read repo context. No repo-local `AGENTS.md`, `CLAUDE.md`, or `GEMINI.md`
  exists in this worktree; `lessons_learnt.md` notes that OWL product-path tests
  need mirrored `wrk_oracle` and Win64 BC45 libs in clean task worktrees.
- Set up ignored local junctions for `wrk_oracle` and
  `target\bc45-libs\win64` so product-path probes can run from the task
  worktree.
- Ran the stock Win64 OWL product matrix. Current first blocker:
  `GROUPBOX` and `EDIT` fail with `no matching overload` on constructors called
  with omitted trailing defaults. Other known gaps remain: GAUGE/LISTBOX/
  COMBOBOX/SLIDER link helpers, STATIC close-time heap corruption, and
  HELLO/POPUP launch crashes.
- Read parser/codegen paths for default-argument recording, overload
  registration, `new` construction, constructor chaining, and synthesized
  copy-constructor coexistence. The likely root cause is default metadata being
  dropped when a sole user constructor is promoted from `sigs.funcs` into
  `sigs.overloads` before adding a synthesized copy constructor.
- Drafted HLD:
  `wrk_docs/2026.06.19 - HLD - preserve constructor defaults during overload promotion.md`.
- Initial `apply_patch` wrote the HLD/journal into the main clone because the
  patch tool used the session cwd. Moved exactly those two files into this task
  worktree and left the pre-existing untracked B-08 main-clone doc untouched.
- Ran three read-only adversarial design reviewers and one refutation pass. The
  surviving findings tightened the helper contract, regression shape,
  product-matrix evidence gate, quality-map traceability requirement, and
  residual risks around extern constructor symbols and stale `overload_defaults`
  expression refresh.
- Arthur approved the HLD on 2026-06-20. Marked the document SIGNED-OFF and
  moved to implementation.
- Added a focused regression for a defaulted sole user constructor promoted to
  coexist with a synthesized copy constructor. It failed before the fix with
  `no matching overload for call to 'C::C'`.
- Implemented shared overload-default lookup and used it when promoting a user
  constructor out of `sigs.funcs`. The regression now passes.
- Reran the OWL product matrix. GROUPBOX no longer fails constructor overload
  resolution; it builds and exits before the window with `0xE0000002`. EDIT no
  longer fails constructor overload resolution; it now reaches a different
  codegen blocker, `no member named 'EditTextLen'`.
- Ran the code-reviewer pass. No blockers or required changes found; the review
  checked the helper contract, constructor-promotion path, regression shape, and
  quality-map/product bookkeeping.

## 2026-06-20

- Resumed after Arthur's approval to continue the next quality-map step.
- Confirmed the task worktree is clean and one commit ahead of `origin/main`.
- Re-read repo-local instructions state: no repo `AGENTS.md`, `CLAUDE.md`, or
  `GEMINI.md` exists in the worktree; `lessons_learnt.md` still highlights the
  OWL fixture/library junction requirement.
- Investigated the post-overload EDIT failure. `TExampleEdit` uses
  `EditTextLen + 1` in a constructor default argument before the class member
  loop reaches `static const unsigned EditTextLen`; the existing parser-side
  unqualified-static lookup is therefore too early. The default is stored as a
  bare `Var("EditTextLen")`, then expanded in the caller's context where
  codegen tries the caller's `this->EditTextLen` and reports
  `no member named 'EditTextLen'`.
- Lightly classified the GROUPBOX follow-on: `0xE0000002` is mdbcc's class
  exception code, so the product now reaches an unhandled OWL class exception at
  launch rather than the previous constructor-overload compiler error.
- Drafted the next HLD:
  `wrk_docs/2026.06.20 - HLD - EDIT static default and GROUPBOX exception classification.md`.
- Ran a sequential adversarial design review. Folded in findings to reuse the
  parser-supported class/base static lookup, rewrite and record-refresh both
  default stores, and allow address-of recursion because the rewrite qualifies
  names rather than replacing them with constants.
- Arthur approved the HLD on 2026-06-21. Marked it SIGNED-OFF and moved to
  implementation.
- Added a regression for a promoted constructor default that references a
  later-declared static member in the declaring class. Before the fix it failed
  in `Maker::make` with `no member named 'Limit'`, matching the EDIT failure
  shape where the default expression was evaluated in the caller's class scope.
- Implemented parser finalization of default-argument static data member
  references. Defaults in both `fn_defaults` and `overload_defaults` now rewrite
  bare static member names to `Class::member` after full class parsing and
  before extern static emission; `overload_defaults` record caches are refreshed
  alongside `fn_defaults`.
- Reran the focused regression, default-argument slice, static-member slice, and
  promoted-constructor slice; all passed.
- Reran the OWL product matrix. EDIT moved from `EditTextLen` codegen failure
  to link failure on `GetFileTitleA`, `GetSaveFileNameA`, `GetWindowDC`,
  `TDC::GetTextExtent`, and `TTinyCaption::DoNCActivate`. GROUPBOX still builds
  and exits before the `GroupBox Tester` window with `0xE0000002`, confirming it
  remains a separate runtime class-exception follow-up.
- Final focused checks stayed green:
  - `mdtimeout 120 -- cargo test -q --test end_to_end promoted_ctor_default_resolves_later_static_member_in_declaring_class -- --nocapture`
  - `mdtimeout 120 -- cargo test -q --test end_to_end promoted_ctor -- --nocapture`
  - `mdtimeout 120 -- cargo test -q --test end_to_end default_arg -- --nocapture`
  - `mdtimeout 120 -- cargo test -q --test end_to_end static -- --nocapture`
  - `mdtimeout 120 -- cargo test -q --test quality_map -- --nocapture`
  - `mdtimeout 600 -- cargo clippy --lib --test end_to_end --test owl_examples_product --test quality_map -- -D warnings`
  - `mdtimeout 900 -- cargo test --release --test owl_examples_product owl_stock_product_matrix_win64 -- --ignored --nocapture --test-threads=1`
- The full fleet integration gate is still red for pre-existing/unrelated repo
  state, so the branch is not safe to push under the requested preconditions:
  - `mdtimeout 120 -- cargo fmt --all -- --check` reports broad formatting drift
    in files outside this task.
  - `mdtimeout 600 -- cargo clippy --all-targets -- -D warnings` reports
    `collapsible_if` / `needless_borrow` warnings in
    `tests/external_corpus_adapter.rs` and `tests/fuzz_reduction_path.rs`.
  - `mdtimeout 900 -- cargo test` fails in `tests/i386_run.rs` with 143 BCC32
    reference-build failures, all showing `bcc32 build failed: exit=Some(1)`
    and empty stderr.
