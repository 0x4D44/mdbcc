# 2026.06.08 — railc link closure 343 → 30; six committed features; the cheap general-correctness wins are done

A long, high-yield session. The all-mdbcc railc link closure fell from **343 →
30** unresolved and OWL source compilation rose **66 → 82 / 119**, across six
committed compiler features — each RUN-verified against bcc32 4.52, each gated on
the FULL unit suite + clippy + the OWL compile sweep (the G15-lesson gate).

## The six features (this session)

| Tag | What | Result |
|---|---|---|
| **#64** (611e75c, prior) | scoped nested classes (DECLARE_STREAMER) | OWL 60→66 |
| **G13** (460acd4) | dependent nested types in fn-templates (`Base::Streamer strmr(base)`) | OWL 66→82 |
| **G14** (c297cb1) | vtable-slot symbols mangle with the record table | closure 51→39 |
| **G15/G15b** (009e6e7, c8bf791) | cross-TU free-fn mangling (#25) + function-as-value | closure 39→35; 3 OWL thunks |
| **G16** (c282d58) | placement array-new of a class with a ctor | closure 35→32 |
| **G17** (ff9347f) | key `overload_defaults` by param types (#25b) | closure 32→30 |

Closure trajectory measured on `railc/*.o + mdcw32.lib + mdbids.lib + mdowl.lib`:
**343 (start) → 51 (G13 + 82-obj mdowl.lib) → 39 (G14) → 35 (G15/b) → 32 (G16)
→ 30 (G17).**

## Two cross-cutting correctness themes

1. **Mangling consistency (G14/G15/G17).** mdbcc's symbol model uses raw names
   for non-overloaded entities and a per-TU placeholder (`R<id>` / arity-only
   default matching) in several spots. Each shortcut was internally consistent
   within a TU but BROKE cross-TU in the all-mdbcc link. The fixes route every
   on-the-wire symbol through the records-aware mangler and key per-overload data
   by TYPES, not ids/arity.
2. **The OWL-sweep gate.** G15 passed the full unit suite + clippy yet regressed
   8 OWL files (caught only by the OWL compile sweep). RULE, now standing: any
   change to mangling / symbol routing / name resolution / overload data MUST be
   gated on `for f in OWL/*.CPP; do bcc.exe -c -m32 -D__WIN32__ ...; done`
   (expect OK ≥ 82) before commit, not just `cargo test`.

## The remaining 30 — all deeper, separate arcs

- **~10 OWL ctors (the dominant wall): Stage-3 data-bearing virtual bases +
  MI** (task #10). TDialog/TDib/TDocManager/TFrameWindow×3/TLayout from
  DIALOG/DIB/DOCMANAG/FRAMEWIN/LAYOUTWI; TApplication/TGdiBase **copy ctors**
  (implicit copy-ctor synthesis); TXOwl×2 (EXCEPT.CPP). These files still fail
  the OWL sweep (37 fails: BITMAPGA…UIHANDLE, frame/dialog/doc/OLE families).
- **8 RTL / C0 startup** — `_C0argc/_C0argv/_C0environ/_ErrorExit/_cleanup/
  _hInstance/_oscmd/_osenv/_terminate/__nextreal/__realcvt/_lrand` (W2: the C0
  startup object + RTL coverage).
- **2 BIDS `TVectorImpBase$S909$S887` / `$S936$S887`** — NOT deferred in any
  railc TU (G16+G17 cleared the body blockers). A CROSS-TU TEMPLATE-INSTANTIATION
  NAMING issue: the `$S<id>` template-argument encodings are per-TU synthetic
  ids, so a reference and a definition in different TUs don't agree on the
  instantiation symbol. Its own arc (analogous to G14's record-tag fix, but for
  template-arg encodings).
- **~4 misc** — HandleGlobalException, TRect::Contains (deferred inline), fputs
  (`p2R0` — FILE typedef mangles as a tagless `R<id>`, a residual type-tag leak),
  strnewdup, to_upper.

## State

Tree clean; 6 features + 3 journals committed; suite 1675/0; clippy 0; 88
byte-identity baselines unchanged; OWL sweep 82/119. railc still does NOT link
(30 unresolved), no exe, no WOW64 run, no golden diff — but the closure is >11×
smaller than session start and every remaining symbol is attributed to one of
four named follow-ons. Next highest-value targets, in order: (1) the RTL/C0
startup glue (8 symbols, W2 — mechanical), (2) the cross-TU template-instantiation
naming (2 symbols, a focused mangling arc), (3) Stage-3 vbases + copy-ctor synth
(task #10, architectural — the dominant wall, worth discussing scope with Arthur
before starting).
