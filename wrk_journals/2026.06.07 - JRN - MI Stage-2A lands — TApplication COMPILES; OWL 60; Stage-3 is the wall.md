# 2026.06.07 — MI Stage-2A lands: TApplication COMPILES; OWL 60/119; Stage-3 is the wall

Sixth arc of the day (22 commits total). The polymorphic-extra-base machinery
is in; both OWL pillars now compile from source.

## MI Stage-2 slice A (d14c203)

Secondary vtables as SYNTHETIC RECORDS (the key design unlock: they ride the
record-id-keyed vtable machinery — codegen RipRef::Vtable, COFF, linker RVA
table — with ZERO linker changes) + deduplicated this-adjusting thunks
(`sub dword [esp+4], off; jmp D::method` — cdecl `this` is a stack arg, so
no register juggling) + SetVptr installing secondaries at [this+off] after
the primary. Win64 keeps the Stage-1 poison (thunks are i386 code); virtual
heritage stays poisoned (Stage-3). RUN: virtual dispatch through an
upcast-adjusted secondary-base pointer reaches the derived override with
`this` correctly restored — 42 == bcc32, first execution.

Documented v1 gaps: `delete` via a secondary-base pointer (frees the
subobject ptr — needs deleting-dtor/offset-to-top), dynamic_cast through a
secondary (slice B; the s4mirtti win64 lock holds since win64 still drops).

## Where OWL stands: 60/119 — and the wall is Stage-3

**TWindow (WINDOW.CPP) and TApplication (APPLICAT.CPP) both compile from
source.** The dominant remaining failure class is now VIRTUAL INHERITANCE:
`TWindow : virtual TEventHandler, virtual TStreamableBase` — every TU that
CONSTRUCTS a window-family object (COMBOBOX, FILEDOC, INPUTDIA, EDITVIEW,
DECFRAME, …) hits the honest Stage-3 poison or the dropped-vbase mem-init
("no member named 'TWindow'"). The same feature unblocks the iostream
diamond (railc LAYOUT.CPP, cout-correctness, DATEIO/TIMEIO).

Smaller residual buckets: G3 slice 2 (DECLARE_STREAMER GetObject chains —
BUTTON/CHECKBOX/EDIT/…), by-value class catch (DIALOG), G9 TAutoEnumerator,
G13 typename-less dependent decls, assorted singles.

## Day total: 22 commits, all oracle-diffed and full-gated

3 codegen features (`_EAX`, array-new/delete[], `_DestructorCount`) · the
15-agent 739-file sweep + REPORT · 5 sweep fixes (fn-ptr cast, paren
declarator, cast-lvalue, pp sizeof, coff Addr32 panic) · MI Stage-1 · G2
(string family complete) · G3s1 (shadowing miscompile killed) · G4s1+G4s2
(closer-base, lexicographic defaults → TWindow) · G5 multi-try SEH3 · G8
i386 MFP dispatch · G11 virtual sret · MI Stage-2A (→ TApplication).

## Next session: G1 Stage-3 — virtual bases

THE wall, everywhere: vbase offset model (Borland ABI), most-derived-
constructs-vbases-once ctor protocol, vbase displacement for member access,
the shared-subobject join for `iostream : istream, ostream` over
`virtual ios`. Design first (fresh context); the staged-MI playbook
(Stage-1/2's additive model + poison gates + synthetic records) is the
template. After Stage-3: G3s2 + singles, then mdar → mdlink+.res → run →
golden diff (W5/W6).
