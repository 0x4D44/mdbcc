# 2026.06.07 — TWindow COMPILES (G4s2 lexicographic overloads); G11 virtual sret; OWL ≈59/119

Fifth arc of the day. Two more oracle-verified increments; the single biggest
OWL TU is in.

## G4 slice 2 — conversion quality dominates defaults (4883a17)

`TMenu popupMenu(hPopupMenu)` was "ambiguous": mdbcc's ADDITIVE score mixing
let one defaults-step equal one conversion-step, tying the EXACT
`TMenu(HMENU, TAutoDelete=)` (0 conv + 1 default) against the
`TMenu(const void*)` POINTER-CONVERSION candidate (1 conv + 0 defaults).
C++ [over.match.best] is lexicographic. One line at the single mixing point:
`score = conv*64 + defaults`. Monotonic ⇒ every pure-conversion ordering
identical; defaults demoted to the tie-breaker S4.2av intended.
Methodology that found it: synthetic ctor-set repro (header-free) + drop-one
bisection → `TMenu(const void*)` was the tying candidate.

**Result: OWL WINDOW.CPP — TWindow, the largest railc-closure item —
COMPILES end-to-end (284 KB i386 COFF).** 21 inline fns deferred on demand
(each a known follow-up: TRect ops lvalue, TVectorImpBase placement-array,
TCommandEnabler bitfield-ish members, TDC vsret-inline…).

## G11 — i386 virtual record-by-value returns (e3a6f52)

The hidden sret lead now flows through the VIRTUAL path: buffer pointer
pushed last (= first stack arg, [ebp+8], before `this`) into the vtable
dispatch; caller cleans +4. RUN: dynamic dispatch via base pointer fills the
caller's buffer (42 = bcc32). GAUGE/TEXTGADG/MESSAGEB compile.

## OWL standings (full-source re-measure, post-G2..G11)

≈59/119 compile (sweep baseline 51), including WINDOW.CPP. Remaining error
classes, by lever size:
1. **MI Stage-2** (task #9): `new TModule/TWindow/TFileInStream` poisons +
   the "no member named 'TWindow/TView/…'" mem-init class (dropped extras) —
   APPLICAT, COMBOBOX, FILEDOC, INPUTDIA, EDITVIEW, DECFRAME, DECMDIFR,
   FLOATFRA, … the dominant bucket.
2. **G3 slice 2**: DECLARE_STREAMER `GetObject()->X` wrong-decl chain —
   BUTTON, CHECKBOX, EDIT, FILTVAL, GROUPBOX, EDITSEAR, …
3. Singles: by-value class catch (DIALOG), G9 TAutoEnumerator (CHGICON,
   INSOBJ), string::string ambiguity (FRAMEWIN:464), DOCMANAG const-bound,
   DOCVIEW ref-to-ptr deref, BUTTONGA decl-vs-expr, GADGETWI encoder shape,
   HSLIDER/VSLIDER "method call on non-class", SLIDER lvalue.

## Session total (the whole day, 20 commits)

`_EAX` · i386 array-new/delete[] · `_DestructorCount` · 15-agent sweep
(739 files mapped, REPORT.md) · fn-ptr cast · paren declarator · cast-lvalue
addr · pp sizeof-#if · coff Addr32 panic · **MI Stage-1** · G2 free-operator
fallback (STRINGIO ⇒ string family complete) · G3s1 shadowing miscompile ·
G4s1 closer-base · **G5 multi-try SEH3** · **G8 i386 MFP dispatch** ·
**G4s2 (TWindow!)** · **G11 virtual sret** — every one bcc32-diffed,
RUN-verified, full-gated, committed atomically.

## Next session

**MI Stage-2** (task #9 — secondary vtables + this-adjusting thunks; the
linker's record-id-keyed vtable-RVA table is the deep ripple; re-flips the
s4mirtti lock) — it is now BOTH the biggest OWL bucket and APPLICAT's gate.
Then G3 slice 2, then the singles, then Stage-3 → mdar → mdlink+.res →
run → golden diff.
