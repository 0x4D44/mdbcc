# HANDOVER — railc self-host campaign (2026.06.10)

For the next agent team. Read this first, then the goal doc
(`wrk_docs\2026.06.06 - GOAL - railc self-hosted (mdbcc compiles railc + OWL
from source).md`), then the campaign journal
(`notes\2026.06.09 - closure 19 campaign (post-Stage-3).md`) which has the
per-fix detail for everything summarized here.

## The goal (verbatim intent)

A fully implemented bcc32 (mdbcc + mdlink, Rust, this repo) compiles railc's
21 TUs AND the OWL/RTL/BIDS library source (`C:\tmp\bc45\SOURCE`) — all from
Borland C++ 4.5 source — into an i386/PE32 `railc.exe` that runs natively on
Win11 x64 under WOW64 and behaves identically to the bcc32+tlink32 golden.
Done ONLY when every byte of railc.exe is mdbcc-built (OS/CTL3D as imports)
and it matches the golden across the GUI parity tiers. Never edit
`C:\language\railc` or the Borland trees (`C:\tmp\bc45`, `wrk_oracle`
goldens). Differential-verify each increment; commit each increment
(user 0x4D44 / martingdavidson@gmail.com); -m32 to both bcc and mdlink;
green gate before every commit = full `cargo test --release` + clippy clean
+ the o1_x86 byte-identity stripes.

## Where we are (HEAD = 58d0e4e)

**Waypoints W0–W4 are DONE. W5 is link-complete (zero unresolved); the
.rsrc/resource-compiler half is in flight. W6 (run & match) has conquered
the entire startup chain — railc.exe runs all the way into its own
application code and fails ONLY because the image has no resources yet.**

Run `bash scripts/closure.sh` — the campaign's oracle. Expected output
today: railc ok=21, owl ok=96, streams ok=48, link exit=0, ZERO unresolved.
It produces `/tmp/rb/railc.exe` (Git-Bash /tmp = `C:\Users\<u>\AppData\Local\Temp`
— careful: `C:\tmp` is a DIFFERENT directory holding the Borland tree).
Relink with `--map railc.map` for the symbolization workflow (below).

Running `/tmp/rb/railc.exe` today: the full RTL `#pragma startup` chain runs
(version→heap→argv→handles→streams→cvt→iostream→tzset), all global ctors
run, WinMain → TApplication::Run → InitInstance → railc's own
TMainWindow ctor → TModule::MakeWindow → TWindow::Create, which THROWS
TXInvalidWindow (window creation fails — **no .rsrc section**: LoadMenu/
LoadIcon fail). The throw is correctly caught by Run's `catch (TXOwl&)`
(cross-TU, derived-as-base — that all works now) and OWL's error path runs
to `HandleGlobalException`, which faults reading the exception's string
(open issue G58 below).

## This session's arc (commits 9c323d4..58d0e4e — read the messages, they
are the documentation)

- 9c323d4 — W6 closure 8→0: bucket fixes G45 (ctor EH pad x64-only gate),
  G46 (SEH3 scope-table rename retarget), Bug C (imports + IAT fn-ptr
  values), Bug D (template deduction from method calls), Bug E (WINVER
  header skew); fallout fixes G47 (MI-join secondary vtables), G48
  (**`#pragma startup`** — the RTL's entire init mechanism, pp→parser→
  thunks→mdlink ordering), G49 (tagless typedef adopts name for linkage —
  bcc32-oracle-verified `@takefoo$qp3FOO`), G50–G52 (default-ctor globals,
  heap/placement-new vbase setup, general static-image data relocs);
  rtlshim grows _cleanup/_terminate/__isGUI/_control87/_qmul10/_fuildq/
  isatty/memchr + C0 seeding; scripts/resweep_rtl.sh persists the sweep.
- 4cffff9 — G50b/c (zero-arg ctor protos; `.mdbcc_ctor.` exempt from the
  WeakExternal fold), G51b (queued-global vbase setup), G53 (out-of-line
  ctors get the member-ctor splice).
- dbbf59e — G54: EH class identity is an ADDRESS compare; vtable/typeinfo
  symbols now emit as canonical `@Tag@3` / `@$xt$…` WeakExternals and
  mdlink's COMDAT fold REDIRECTS losers (true link.exe semantics).
- 47cea63 — G55 (i386 SEH3 derived-as-base walk via the `[vtable-4]`
  descriptor word + poly flag in args[2]) + G57 (derived→base ref ARG
  binding adds base_offset/vbptr).
- ad7a6bc — W5 rc gaps 1–4 (see below).
- 58d0e4e — G57b (decl-init + return ref-binding sites).

## The immediate blocking work: the rc compiler (task #10)

`notes\rc_inventory_report.txt` is a COMPLETE, verified implementation spec
(committed). It inventories src/rc today, everything RAILC.RC needs, the
golden `.res` parsed entry-by-entry, the integration seam, and a 10-step
gap list with acceptance criteria. **Gaps 1–4 are done (ad7a6bc)**:
`rc::compile_file(Path, &defines)` preprocesses (+`#define`/`#include`/
`#ifdef`) and lexes `C:\language\railc\RESOURCE\RAILC.RC` fully; parsing
stops at `MAIN_MENU MENU` (named ids).

**Remaining: gaps 5–10**, in the report's order:
5. Named ResIds + style expressions + WS_/DS_/BS_/SS_ symbol table + ICON
   control shorthand; fix the forced-0x40010000 shorthand-style rule.
6. .res writer fidelity profile (source order, RT_STRING last, lang 0x0000
   default, per-type memflags) — keep the existing brc32-5.40 differential
   corpus (tests/rc_res.rs, 28 tests) green by making this a PROFILE.
7. ICON/BITMAP/RCDATA (file refs relative to the .rc + inline raw-hex;
   .ico split into RT_ICON + synthesized GRPICONDIR; BMP header-computed
   strip).
8. VERSIONINFO.
9. THE acceptance test: compile RESOURCE\RAILC.RC end-to-end, write .res,
   byte-compare to `wrk_oracle\railc_golden\railc.res` (121040 bytes;
   28 records; `wrk_oracle\railc_golden\dump_res.py` parses it).
10. PE integration: generalize pe_writer's emit_rsrc to flat entries with
   NAMED directory entries; add .res input to mdlink (`Input::ResFile` is
   stubbed); then `mdlink -m32 *.o … railc.res` and the exe's menus/icons/
   dialogs load. (The PE32 DataDirectory bug it depends on is FIXED.)

A workflow for this exists and is resumable:
`Workflow({scriptPath: "C:\Users\marti\.claude\projects\C--language-mdbcc\1823f32f-0c8b-4753-becc-83b1e38c0c52\workflows\scripts\rc-compiler-railc-wf_5a36b7a6-b30.js", resumeFromRunId: "wf_5a36b7a6-b30"})`
— the inventory agent's result is cached; the implement agent (worktree,
TDD vs the golden) re-runs. It has TWICE died on the Claude session-token
limit (last reset 7:20pm Europe/London) without committing anything — if
that recurs, implement gaps 5–10 on the spine directly from the report.

## After resources land

1. Relink railc.exe with the .res; TWindow::Create should succeed; the
   main window should appear. Use `mdscreensnap` to capture; the golden
   screenshot is `wrk_oracle\railc_golden\golden_main.png` (plus the
   golden exe `railc.exe` + `railc.map` + link.rsp in that directory, and
   a `run` script).
2. Then the GUI parity tiers per the goal doc (visual diff, message-level
   behaviour, file I/O — railc reads timetable data; check
   `C:\language\railc` for .TT/data files and how the golden is launched).

## Open issues (journaled, ordered by likely next impact)

- **G58**: the OWL error path (only reachable when something throws)
  faults in HandleGlobalException: the caught xmsg's `str` points at a
  heap string whose TStringRef ptr is uninitialized-heap garbage. Suspects:
  string-returning-by-value marshalling in the TXWindow message-building
  chain (ResourceIdToString, operator+), or the throw-site bitwise buffer
  copy (no copy-ctor semantics — see gen_throw_class_i386 step 2).
  Diagnose: break @HandleGlobalException; x=[esp+4]; str=[x]; p=[str].
- The thrown-object buffer copy is BITWISE (memcpy) — correct C++ copy-
  constructs. Related to G58. The catch-side never destroys the caught
  object either (refcount leak, benign).
- x64 marshal_args has NOT been audited for the G57 family (i386-only
  fixes); x64 cross-TU EH also still walks the catching frame's typeinfo
  table (the G55 descriptor-walk is i386-only).
- Default-ctor file-scope globals construct now (G50), but ARRAYS of
  ctor-classes (`G g[3];`) still don't.
- `_control87` is a software-CW shim (no real x87 effect); `_fuildq`
  loses >2^53 mantissa bits; both could shift scanf/strtod parity.
- EXCEPT.C needs the `_EAX` pseudo-register (parked); DEFHANDL.C excluded
  (would duplicate GP.C's `__DefHandler`).
- o6_e6_ddvt gui test is FLAKY under full-suite parallel load only.
- Bug C layer 3 (principled `!proto.c_linkage` in force_cpp) parked —
  needs parser C-TU mode.

## Mechanics you need

- **Cycle**: edit → `cargo build --release` → `bash scripts/resweep_rtl.sh`
  (rebuilds wrk_oracle/{rtl_objs,bids_objs} + mdcw32.lib/mdbids.lib with
  the CURRENT compiler — required whenever codegen changes) →
  `bash scripts/closure.sh` → relink with --map → run under
  `/usr/bin/timeout N ./railc.exe`.
- **Crash debugging**: gdb -batch -ex run -ex bt, then symbolize against
  railc.map (sorted `VA name` lines; bisect right). Static fns attribute
  to the previous public symbol — large +0xNNN offsets mean "an inline/
  static after that symbol", don't trust them blindly. Watch the
  arg-slot arithmetic at a breakpoint: at function ENTRY [esp]=ret,
  [esp+4]=arg1/this, [esp+8]=arg2 (a mistake here cost an hour today).
- **bcc32 oracle**: `PATH="/c/tmp/bc45/BIN:$PATH" /usr/bin/timeout 60
  bcc32 …` with JOINED -I args. The i386_run differential harness
  (check_i386_cpp) runs both compilers and compares exit codes (target 42).
- **Probing style**: minimal /tmp repro first (the G-fix probes in this
  session: tins.cpp, baseadj*.cpp, eh2tu/*, minit*.cpp, refsites.cpp —
  every fix above started as a ≤20-line probe), then the fix, then a
  differential test in tests/i386_run.rs (220 tests there now), then the
  full gate.
- **Tests**: `cargo test --release` ≈ 1725 tests, all green at HEAD;
  clippy clean; o1_x86_byte_identity re-bless ONLY with a journal note in
  the same commit.

## Task list state

#9 (startup segfault) and #11 (closure 8→0) completed; #10 (rc compiler)
in_progress, carries the same status text as this brief.
