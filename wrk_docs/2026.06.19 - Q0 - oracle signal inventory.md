# Q0 - Oracle Signal Inventory

Date: 2026-06-19
Goal: `2026.06.19 - GOAL - compiler quality and oracle expansion.md`
Mode: Build/test-infra progress toward Q0/Q1/Q2.

## Compact Inventory

| Oracle | Prerequisites | Current floor | Known gaps | Mode / skip contract |
|---|---|---:|---|---|
| O1 executable and byte-identity stripes (`tests/end_to_end.rs`, `tests/o1_*byte_identity*.rs`) | None | Hand-expected runtime plus byte baselines | Curated point corpus, not broad real-program coverage | Always-on; no skip contract |
| O2/O3 differential corpus (`tests/differential.rs`) | MSVC `cl` and/or Borland `bcc32` 5.5.1 | Zero external active is a hard oracle-health halt | Fixture-boundary skip directives filter unsupported/reference-noisy cases | Mixed; tool absence is environment state |
| O12 i386 portable corpus (`tests/oracle_o12_x86_corpus.rs`) | BC4.52 BCC32/TLINK | MATCH >= 20 | Prints COMPILE-GAP/MISMATCH/RUN-SKIP/REF-GAP table | Self-skipping; BC4.52 absence is environment state |
| O12 i386 88-program e2e (`tests/oracle_o12_x86_e2e.rs`) | BC4.52 BCC32/TLINK | MATCH >= 83/88 | Remaining 5 are reference gaps: inline asm/TASM, C89 mid-block declaration, private base-member access | Self-skipping; reference gaps are not mdbcc backlog |
| O13 COFF/OMF parity (`tests/oracle_o13_coff_parity.rs`) | BC4.52 BCC32 | 13 symbol-parity fixtures | Section-size and COMDEF differences are advisory | Self-skipping; BC4.52 absence is environment state |
| O14 OMF decode/link wiring (`tests/oracle_o14_omf_link.rs`) | BC4.52 BCC32 | 5 OMF decode fixtures plus OMF input sentinel | Full runnable PE oracle waits on i386 PE writer | Self-skipping; BC4.52 absence is environment state |
| O15 header preprocess (`tests/oracle_o15_header_acceptance.rs`) | BC4.52 INCLUDE tree | ACCEPT >= 241/246 | 5 remaining standalone header-context/reference guards | Self-skipping; INCLUDE absence is environment state |
| Real-header compile/parse (`tests/oracle_real_headers.rs`) | BC4.52 INCLUDE tree; optional BCC32 | C parse >= 112/246; C++ parse >= 126/246 | Many standalone rejects need `windows.h` context; genuine gaps remain in C++ forms and function-pointer declarators | Self-skipping; mdbcc-only run is partial evidence |
| S4 ClassLib and MI/RTTI (`tests/oracle_s4_classlib.rs`) | BC4.52 INCLUDE tree; optional BCC32 | ClassLib run 18, MI/RTTI run 20, date overload compile pin | `TArrayAsVector<int>` is a pinned bcc32 reference ambiguity | Self-skipping; BCC absence weakens diff arm |
| Stock OWL product matrix (`tests/owl_examples_product.rs`) | BC4.52 source tree, Win64 BC45 libs, desktop | 11 stock entries; BUTTON and INSTANCE green baselines | STATIC close heap corruption; HELLO/POPUP launch crash; GROUPBOX/EDIT overload/codegen; GAUGE/LISTBOX/COMBOBOX/SLIDER link helpers | Ignored/manual; missing fixtures/libs/desktop are environment state |
| RailC source + Win64 OWL smoke (`tests/railc_source_slice.rs`) | Arthur's RailC corpus, release tools, BC45 libs, optional GUI golden | Manifest, compile, Win64 link, runtime/dialog/KINGSX parity gates | Local-corpus ignored gate; KINGSX has explicit oracle-skip when golden fixtures absent | Ignored/manual; missing corpus/golden is environment state |
| Resource byte oracles (`tests/rc_res.rs`, `tests/rc_railc.rs`, `tests/pe_rsrc.rs`) | BRC32 for differential arm; optional RailC golden `.res` | BRC byte differentials plus hand-golden/RailC parity | BRC/golden absence weakens evidence | Mixed; external/golden absence is environment state |

## Duplicate Classifications To Unify

- O12 corpus and O12 e2e duplicate MATCH/MISMATCH/COMPILE-GAP/RUN-SKIP/REF-GAP status logic.
- BC4.52 absent self-skip strings recur across O13/O14/O15/real-header/S4 suites.
- Stock OWL failures were duplicated between `scratchpad.md` and the two-entry product test; `tests/owl_examples_product.rs` now holds the canonical test-side matrix.

## Soft Or Partial Skips

- `oracle_real_headers` can run mdbcc-only when BCC32 is absent; the map labels that as partial differential evidence.
- `railc_source_slice` skips KINGSX parity when golden fixtures or `pwsh` are absent; the map labels that as environment state.
- `owl_examples_product` can build-only when no interactive desktop is present; the map does not call that full window-smoke success.

## First Ranked Backlog

The generated backlog is `tests/quality_map.rs::quality_map_prints_ranked_compiler_backlog`. Current top items:

1. Classify and fix GROUPBOX/EDIT constructor overload failures.
2. Resolve OWL/GDI helper link gaps for GAUGE/LISTBOX/COMBOBOX/SLIDER.
3. Root-cause STATIC close-time heap corruption.
4. Root-cause HELLO/POPUP launch crash before first window.
5. Add generated ABI/calling-convention torture around historical Win64/i386 bugs.
6. Add structural invariants for x64 stack/call frames and object symbols.
7. Lift real-header C++ parse acceptance beyond 126 with context-aware gap split.
8. Build the first curated external C corpus adapter.
9. Prove fuzz-generate/diff/reduce/regress path end to end.
10. Complete O14 runnable i386 OMF-to-PE oracle.

Reference-only gaps are kept out of the mdbcc backlog; the first explicit entry is the O12 REF-GAP set in `tests/oracle_o12_x86_e2e.rs`.

## Verification

- `mdtimeout 240 -- cargo test --no-run --test quality_map --test owl_examples_product`
- `mdtimeout 120 -- cargo test -q --test quality_map -- --nocapture`
- `mdtimeout 120 -- cargo test -q --test owl_examples_product owl_product_matrix_inventory_has_q2_shape -- --nocapture`
- `mdtimeout 120 -- cargo test -q --test owl_examples_product owl_stock_product_matrix_win64 -- --ignored --nocapture --test-threads=1`

The ignored OWL product run in this worktree reported only `env-skip` rows because `wrk_oracle` is gitignored and absent from the task worktree; this is expected environment evidence, not product success.
