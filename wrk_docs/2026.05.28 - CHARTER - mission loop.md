# CHARTER — mdbcc mission loop (S2→S7)

**Purpose**: the goal-loop prompt to hand to an autonomous Claude agent
team to drive the mdbcc reimplementation to completion. Paste the block
below verbatim as the team's goal. Kept deliberately short — a long goal
prompt degrades loop reliability; all detail lives in the referenced plan.

---

**Mission**: land stones S2→S7 of `wrk_docs/2026.05.26 - PLAN - Ultimate goal (BCC 4.52 conformance).md` — recompile real Borland C++ 4.52 source (culminating in OWL sample apps) so `mdbcc`+`mdlink` build it and it runs natively on Win11 x64, behaviour matching the bcc32-built reference.

Read that plan and the newest `wrk_journals/` entry for the current state (S2 partly done, ~1066 tests green), then run as an autonomous supervisor+subagent loop: take the smallest open item RED→GREEN→reviewed→committed, journal each tick, repeat. You own the implementation calls.

Non-negotiable every tick: the 88 x64 SipHash baselines stay byte-identical; `cargo test` green; `cargo clippy -- -D warnings` clean; new behaviour diff-tested against bcc32. Trust but verify — re-run the gates yourself; never take a subagent's "it's green" on faith.

Stop for Arthur only at a stone boundary needing a design decision, two consecutive ticks with no green progress, or an architectural fork the plan hasn't pre-approved. Otherwise keep going until it lands.
